---
title: Group-Graph Policy Optimization for Long-Horizon Agentic Reinforcement Learning
title_zh: 面向长周期智能体强化学习的组图策略优化
authors: "Yunan Wang, Minghui Song, Zihan Zhang, Shaohan Huang, Haizhen Huang, Furu Wei, Weiwei Deng, Feng Sun, Qi Zhang"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22995v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 面向长程代理强化学习的组图策略优化
tldr: "长程智能体强化学习面临奖励稀疏与延迟，现有步级方法因忽略状态转移的图结构导致高方差估计和局部信用分配。本文提出G2PO，将线性交互轨迹转化为全局状态转移图，通过聚合相同观测降低采样方差，并设计边中心优势估计在全图标准化TD误差以识别关键转移。在WebShop、ALFWorld等基准上，G2PO成功率提升高达22.2%，显著超越GRPO等基线。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22995v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 689, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1435, \"height\": 688, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 393, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1447, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 555, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 699, \"height\": 320, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 728, \"height\": 322, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1449, \"height\": 277, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22995v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1442, \"height\": 832, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22995v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 590, \"height\": 323, \"label\": \"Table\"}]"
motivation: 长程智能体任务中奖励稀疏延迟，现有步级强化学习忽略状态图结构，导致高方差状态值估计和局部短视的信用分配。
method: G2PO将线性轨迹转为全局状态转移图，聚合相同观测进行状态值估计，并采用边中心优势估计全局标准化TD误差以聚焦关键转移。
result: "在WebShop、ALFWorld和AppWorld基准上，G2PO成功率提升高达22.2%，显著优于GRPO等基线。"
conclusion: G2PO通过图结构信用分配有效缓解长程RL奖励稀疏问题，提升智能体探索效率与性能。
---

## 摘要
基于组的强化学习（RL）显著增强了大型语言模型（LLM）在智能体场景中的表现。为实现更细粒度的策略更新，最近的智能体强化学习框架已从轨迹级训练转向步骤级训练。然而，长周期智能体强化学习面临严重的奖励稀疏和延迟问题，因为反馈通常被推迟数十个交互步骤。虽然现有的步骤级框架细化了训练粒度，但其信用分配仍然是粗粒度的，并且仍然将智能体探索视为孤立的线性轨迹。这种过于简化的视角忽略了状态转移的内在图结构，导致高方差的状态值估计和短视、局部的信用分配。为了克服这些关键瓶颈，我们提出了组图策略优化（G2PO），这是一种专为多轮智能体任务设计的新型基于组的强化学习算法。G2PO明确地将线性交互轨迹转换为全局状态转移图。通过聚合不同轨迹中的相同观测，我们引入了组聚合状态值估计，从而降低了采样方差和轨迹依赖偏差。此外，我们重新定义了智能体动作作为状态节点之间的转移，并提出了一种以边为中心的优势估计策略。通过全局标准化整个图中的时序差分（TD）误差，G2PO明确识别并优先处理推动绝对任务进展的关键转移。在代表性的长周期基准测试——WebShop、ALFWorld和AppWorld上的大量实验表明，G2PO显著优于最先进的基于提示和强化学习的基线方法，成功率提升高达22.2%（相对于GRPO）。

## Abstract
Group-based Reinforcement Learning (RL) has significantly enhanced Large Language Models (LLMs) in agentic scenarios. To achieve finer-grained policy updates, recent agentic RL frameworks have shifted from trajectory-level to step-level training. However, long-horizon agentic RL suffers from severe reward sparsity and delay, as feedback is often deferred for dozens of interaction steps. While existing step-level frameworks refine training granularity, their credit assignment remains coarse-grained and still treats agent exploration as isolated, linear trajectories. This oversimplified perspective ignores the inherent graph structure of state transitions, leading to high-variance state-value estimation and myopic, localized credit assignment. To overcome these critical bottlenecks, we propose Group-Graph Policy Optimization (G2PO), a novel group-based RL algorithm tailored for multi-turn agentic tasks. G2PO explicitly transforms linear interaction trajectories into a global state-transition graph. By aggregating identical observations across different trajectories, we introduce group-aggregation state-value estimation that reduces sampling variance and trajectory-dependent bias. Furthermore, we redefine agent actions as transitions between state nodes and propose an edge-centric advantage estimation strategy. By globally standardizing Temporal Difference (TD) errors across the entire graph, G2PO explicitly identifies and prioritizes critical transitions that drive absolute task progress. Extensive experiments on representative long-horizon benchmarks-WebShop, ALFWorld, and AppWorld-demonstrate that G2PO substantially outperforms state-of-the-art prompt-based and RL baselines, achieving remarkable success rate improvements of up to 22.2% over GRPO.

---

## 论文详细总结（自动生成）

# 论文总结：Group-Graph Policy Optimization for Long-Horizon Agentic Reinforcement Learning (G2PO)

## 1. 核心问题与整体含义（研究动机和背景）

- **问题背景**：大型语言模型（LLM）在智能体场景中需与环境进行多轮、长周期交互（如WebShop、ALFWorld），但面临严重的**奖励稀疏与延迟**问题：最终奖励仅在整个轨迹结束时获得，中间步骤无即时反馈。现有方法分为两类：
  - **轨迹级训练**（如RAGEN、Search-R1）：将整个轨迹作为训练样本，导致上下文爆炸和内存消耗过大。
  - **步骤级训练**（如GiGPO）：将每一步的观测-动作对作为独立样本，缓解了上下文问题，但信用分配仍为粗粒度。
- **关键瓶颈**：现有步骤级方法将智能体探索视为孤立的线性轨迹，忽略状态转移的**内在图结构**。这导致两个问题：
  - (i) **状态值估计高方差**：相同状态在不同轨迹中可能因后续随机性得出不同回报，单次采样估计不可靠。
  - (ii) **局部组范式视野有限**：动作仅在局部状态下与其他动作比较，无法衡量其对全局目标的绝对贡献。
- **本文目标**：提出G2PO，将线性轨迹转化为**全局状态转移图**，通过聚合相同观测降低方差，通过边中心优势估计识别关键转移，从而提升长周期智能体任务性能。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：将多条采样轨迹的观测通过聚类（相同或相似观测）构建为**状态组图**，图中节点是状态组，边是动作（状态转移）。在此基础上进行：
  - (1) **组聚合状态值估计**：对于每个状态组 \(G_k\)，其值定义为该组内所有步骤的折扣回报平均值，而非依赖单轨迹回报。公式：  
    \[
    V(G_k) = \frac{1}{|G_k|} \sum_{o_i^j \in G_k} \gamma^{T-j+1} R_i
    \]
    这样可显著降低方差。
  - (2) **边中心优势估计**：将动作视为从源节点到目标节点的边，用全局标准化后的时序差分（TD）误差作为优势：
    \[
    \delta_{ij} = V(G_{k'}) - V(G_k)
    \]
    \[
    A^{EC}(a_{ij}) = \frac{\delta_{ij} - \text{mean}(\{\delta_{ij}\})}{\text{std}(\{\delta_{ij}\})}
    \]
    从而全局地放大关键转移的贡献。
  - (3) **节点中心优势估计**：在同一状态组内，比较不同动作导致的下一状态值，局部标准化。
  - (4) **轨迹级优势（保持与GRPO相同）**：基于最终奖励的全局标准化。
- **最终优势**：将上述三者加权组合：\(A_{ij} = A^{EP}(a_{ij}) + w \cdot (A^{NC}(a_{ij}) + A^{EC}(a_{ij}))\)。
- **目标函数**：沿用GRPO的clip+KL正则化形式，但应用在每一步（步骤级更新）。

## 3. 实验设计

- **数据集/场景**：
  - **WebShop**：电商网站模拟，需搜索、导航、选择商品属性。
  - **ALFWorld**：家居环境任务（6个子任务：Pick、Look、Clean、Heat、Cool、Pick2）。
  - **AppWorld**：通过Python API与多应用交互的复杂软件生态。
- **基准对比方法**：
  - **前沿离线模型**：Qwen3.5-397B、DeepSeek-V3.2、Gemini-2.5-Pro。
  - **提示方法**：Qwen2.5基线、ReAct、Reflexion。
  - **RL训练方法**：PPO（带critic）、RLOO、GRPO、GiGPO。
- **模型规模**：Qwen2.5-1.5B-Instruct、Qwen2.5-7B-Instruct、Qwen2.5-14B-Instruct（仅AppWorld）。

## 4. 资源与算力

- **文中明确说明**：
  - 对于ALFWorld和WebShop：使用 **4×H100 GPU**，训练 **100 次迭代**。
  - 对于AppWorld（14B模型）：使用 **8×H100 GPU**，训练 **50 次迭代**。
  - 每组实验采用 **128个环境**并行采样（16组×8个环境/组）。
- 未提及总训练时长（小时数），但给出了单步时间分解（图5）：G2PO的优势计算仅增加约1秒（CPU上完成），占总时间0.4%，几乎无额外开销。

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验：WebShop + ALFWorld（1.5B和7B两个规模×多个子任务），AppWorld（14B一个规模）。
  - 消融实验：在ALFWorld和WebShop上逐步添加AEP、ANC、GA、AEC组件。
  - 超参数分析：参数w（平衡权值）的影响。
  - 方差分析：展示状态值估计的方差分布。
  - 效率分析：对比推理步数。
- **充分性评价**：
  - 覆盖面广：三个不同难度的长周期基准，多种模型规模，多个对比方法。
  - 公平性：所有RL方法使用相同超参数和提示模板，组大小统一为8。
  - 统计稳定性：结果报告为3个随机种子的均值±标准差（表1、表2中均有体现）。
  - 消融系统：逐一验证各组件贡献，逻辑清晰。
  - 局限：未在更多框架（如verl-agent以外）或树采样、异步采样等设置下验证（作者在limitation中提及）。

## 6. 主要结论与发现

- **性能提升**：G2PO在所有基准上一致超越所有对比方法。
  - 在WebShop上，相对于GRPO：1.5B模型成功率提升**22.2%**，7B模型提升**19.3%**。
  - 在ALFWorld上，总体成功率提升：1.5B模型**+14.4%**，7B模型**+12.2%**。
  - 在AppWorld上，G2PO取得最高成功率27.6%，优于GRPO（24.8%）和GiGPO（25.7%）。
- **效率提升**：G2PO训练的模型在完成任务时所需交互步数更少（比GRPO和GiGPO均少），降低了推理成本。
- **组件有效性**：组聚合状态值估计和边中心优势估计均带来显著增益，缺一不可。

## 7. 优点

- **方法论创新**：首次将智能体探索的图结构显式纳入信用分配，提出组聚合状态值和边中心优势两个新颖设计。
- **有效解决高方差**：通过聚合相同状态的不同轨迹结果，方差降低至单轨迹的1/|G_k|，实验证实了必要性（图4b）。
- **全局视角**：边中心优势在全图标准化，能够识别并放大关键突破动作，超越局部比较的局限性（案例图11）。
- **计算开销极低**：优势计算在CPU完成，仅增加1秒/步，近乎零额外成本。
- **实验扎实**：多基准、多规模、多对比、带消融和统计分析，结果可靠。
- **代码开源**：提供GitHub仓库。

## 8. 不足与局限

- **框架依赖**：仅在verl-agent框架上实现，未在其他智能体RL框架（如RAGEN、Agent Lightning）上验证，泛化性需进一步检验。
- **采样方式**：当前仅支持同步批量采样，未尝试树采样或异步采样，可能限制了在更复杂探索策略下的适用性。
- **观测聚类假设**：假设相同观测即代表相同状态，但在某些环境中可能存在观测相同但语义状态不同的情况（如隐藏信息差异），可能导致分组不精确。
- **手动设计权值w**：需手动调整步级与轨级优势的平衡，虽然存在最优值（w=0.8），但不同任务可能需不同配置。
- **理论分析有限**：附录B给出了方差降低的证明，但假设较强（相同状态构型完全独立），实际中可能不严格成立。
- **未讨论负面迁移**：未分析当组大小较小时（如组大小为1）G2PO是否退化为普通方法，或可能引入新的偏差。

（完）
