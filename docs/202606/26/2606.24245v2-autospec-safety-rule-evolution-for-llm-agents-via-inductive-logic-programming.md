---
title: "AutoSpec: Safety Rule Evolution for LLM Agents via Inductive Logic Programming"
title_zh: AutoSpec：通过归纳逻辑编程的LLM智能体安全规则演化
authors: "Pingchuan Ma, Zhaoyu Wang, Zimo Ji, Yuguang Zhou, Zhantong Xue, Zongjie Li, Shuai Wang, Xiaoqin Zhang"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24245v2"
tags: ["query:agent-safety"]
score: 9.0
evidence: 通过归纳逻辑编程演化LLM智能体的安全规则
tldr: "大型语言模型智能体在自动化任务中存在安全风险，现有手工规则要么过于保守导致高假阳性，要么过于宽松导致高假阴性。AutoSpec框架基于反例引导归纳合成和归纳逻辑编程，自动进化专家设计的规则：从初始规则和用户标注轨迹出发，迭代挖掘假阳性和假阴性反例，利用ILP学习区分谓词并生成规则编辑。在两个领域291条轨迹上，F1分别达到0.98和0.93，假阳性降低94%，收敛快速，且规则可读、可审计。该框架有效平衡了规则的可解释性和性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24245v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 629, \"height\": 475, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 745, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 867, \"height\": 365, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 878, \"height\": 667, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 661, \"height\": 368, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24245v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 839, \"height\": 297, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 558, \"height\": 289, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 810, \"height\": 566, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 834, \"height\": 256, \"label\": \"Table\"}]"
motivation: LLM智能体安全规则面临可解释性与性能的权衡：手工规则脆弱，神经分类器缺乏可解释性。
method: 通过CEGIS框架结合ILP，从专家规则和标注轨迹中迭代挖掘反例，学习谓词区分，生成并验证规则编辑。
result: "在两个领域规则F1达0.98和0.93，假阳性降低94%，收敛于4-5次迭代，比启发式CEGIS F1高4.8倍。"
conclusion: AutoSpec自动进化出可读、可审计的规则，在保持高召回率的同时大幅降低假阳性，适用于安全关键部署。
---

## 摘要
大型语言模型（LLM）智能体通过将语言模型与外部工具和环境集成，日益自动化复杂任务。然而，它们的自主性带来了显著的安全风险：智能体可能执行破坏性命令、泄露敏感数据或违反领域约束。现有的安全方法面临基本权衡：手工制定的规则可解释但脆弱，过于保守的规则会阻止安全操作（高假正例），而宽松的规则会遗漏不安全行为（高假负例）。神经分类器缺乏安全关键部署所需的可解释性。
我们提出AutoSpec，一个通过反例引导的归纳综合（CEGIS）并结合归纳逻辑编程（ILP）自动从用户安全/不安全标注中演化专家设计的安全规则的框架。从专家规则和带标注的轨迹流开始，AutoSpec迭代评估规则，挖掘假正例和假负例反例，使用ILP学习哪些谓词区分它们，生成候选规则编辑，并验证候选规则以选择最佳修订。关键洞察是ILP高效地识别在假负例中出现频繁但在假正例中很少出现（或反之）的谓词，大幅剪枝了规则编辑的指数级搜索空间。该过程持续直到收敛，产生平衡精确率和召回率的可解释规则。
我们在291条执行轨迹上评估AutoSpec，涵盖代码执行和具身智能体领域。AutoSpec在两个领域上将规则F1提升至0.98和0.93，实现了高达94%的假正例减少，同时保持高召回率，并在4-5次迭代内收敛。ILP引导的方法比启发式CEGIS实现了高达4.8倍的F1提升。学习到的规则是人类可读、可审计的，并能泛化到未见场景。

## Abstract
Large language model (LLM) agents increasingly automate complex tasks by integrating language models with external tools and environments. However, their autonomy poses significant safety risks: agents may execute destructive commands, leak sensitive data, or violate domain constraints. Existing safety approaches face a fundamental tradeoff: hand-crafted rules are interpretable but brittle, with overly conservative rules blocking safe operations (high false positives) while permissive rules miss unsafe behaviors (high false negatives). Neural classifiers lack the interpretability required for safety-critical deployments.   We present AutoSpec, a framework that automatically evolves deployed expert-designed safety rules from user safe/unsafe annotations through counterexample-guided inductive synthesis (CEGIS) guided by inductive logic programming (ILP). Starting from the expert rules and a stream of annotated traces, AutoSpec iteratively evaluates rules, mines false-positive and false-negative counterexamples, uses ILP to learn which predicates discriminate them, generates candidate rule edits, and verifies candidates to select the best revision. The key insight is that ILP efficiently identifies predicates that appear frequently in false negatives but rarely in false positives (or vice versa), dramatically pruning the exponential search space of rule edits. This continues until convergence, producing interpretable rules that balance precision and recall.   We evaluate AutoSpec on 291 execution traces spanning code execution and embodied agent domains. AutoSpec raises rule F1 to 0.98 and 0.93 across the two domains, achieving up to 94% false positive reduction while maintaining high recall, and converges within 4-5 iterations. The ILP-guided approach achieves up to 4.8x higher F1 than heuristic CEGIS. The learned rules are human-readable, auditable, and generalize to unseen scenarios.

---

## 论文详细总结（自动生成）

# AutoSpec 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：LLM 智能体（Code Agent、Embodied Agent 等）在自主执行任务时面临重大安全隐患，如执行 `rm -rf /`、泄露敏感数据、违反领域约束等。
- **现有方法的权衡**：
  - 手工安全规则（如 AgentSpec）可解释、可审计，但**脆弱**：过于保守导致高假正例（阻塞合法操作），过于宽松导致高假负例（遗漏真危险）。
  - 神经分类器（如 Llama Guard）自适应更强，但**黑箱**，无法解释为何拦截，不适合安全关键场景。
- **关键观察**：生产环境中智能体运行产生的用户标注（安全/不安全）可以自动反哺规则，使专家规则能够随环境、模型、工具变化而演化，这正是本文解决的问题。

## 2. 方法论：核心思想、关键技术细节、算法流程

### 2.1 核心思想
- 将安全规则维护问题建模为 **反例引导的归纳综合（CEGIS）**：人类标注作为反例（假正例、假负例），CEGIS 循环迭代合成能正确处理反例的规则。
- 用 **归纳逻辑编程（ILP）** 剪枝指数级搜索空间：ILP 从反例中学习哪些谓词（如 `destructive_os_inst`、`near_danger_zone`）能区分不安全与安全，大幅减少候选编辑的枚举数量。

### 2.2 关键技术细节
- **谓词库** `P`：每个谓词是一个布尔函数，评估事件属性（代码域：`destructive_os_inst`、`involve_system_file`；具身域：`near_danger_zone`、`handling_fragile_object`）。
- **规则结构**：规则 `r(e)` 是谓词逻辑公式，作用于单个事件。轨迹被标为不安全当且仅当存在某条规则在某事件上触发。
- **规则编辑操作**（表2）：
  - `AddConjunct`：添加合取项（收紧）
  - `AddException`：通过 `¬p` 添加例外（收紧）
  - `AddDisjunct`：添加析取分支（放宽）
  - `Relax`：删除文字（放宽）
- **ILP 学习**：使用 ILASP [12] 求解器，将假负例（FN）作为正例、假正例（FP）作为负例，学习最简单的谓词组合（Horn 子句或形式），这些谓词即为区分反例的关键。
- **候选生成与选择**：基于 ILP 输出的建议，生成单步编辑、混合修复链（先收紧后放宽）、失败驱动分支（精确受约束的贪心覆盖）。使用词典序评分 `(F1, Precision, -U, -|R'|)` 选择最佳候选。
- **收敛保证**：每轮贪心提升，且满足局部最优性；多样性压力（对已有谓词降权）防止振荡。

### 2.3 算法流程（如 Alg.1）
1. 从初始专家规则 `R0` 开始。
2. 在带标注轨迹数据集 `D_L` 上评估当前规则，挖掘假正例和假负例。
3. 若无反例，返回当前规则。
4. 运行 ILP 学习区分性谓词，生成编辑建议。
5. 根据建议生成候选规则集。
6. 验证所有候选，选择评分最高的候选。
7. 若新规则评分提高则更新，否则终止。
8. 重复直到收敛、达到目标分数或最大迭代次数。

## 3. 实验设计：数据集、基准、对比方法

### 3.1 数据集
- **代码执行域**：191条轨迹（91条不安全来自 RedCode-Exec 基准，100条安全由 GPT-5.1-Codex 生成）。事件长度 1-9（均值 1.4）。
- **具身智能体域**：100条轨迹（52% 不安全，来自 SafeAgentBench 模拟家庭机器人，使用 AI2-THOR 环境）。事件长度 1-15（均值 5.1）。

### 3.2 基准（Baselines）
- **B1**：专家设计的静态规则（AgentSpec）。
- **B2**：LLM 分类器（使用 5-shot 示例分类）。
- **B3**：不带 ILP 的 CEGIS（基于覆盖率的启发式谓词排名）。
- **B4**：随机搜索（固定预算下随机采样候选）。

### 3.3 实验协议
- RQ1（有效性）：训练/测试划分，报告测试集上的精确率、召回率、F1。
- RQ2（效率）：报告收敛曲线（种子规则从低 F1 开始）。
- RQ3（泛化）：70% 任务学习，30% 新任务评估。
- 消融实验：剔除 ILP、剔除多步编辑、剔除失败驱动分支。

## 4. 资源与算力
- **未使用 GPU 训练**：文中明确指出所有实验在 **MacBook Pro (M2 Max, 32GB RAM)** 上运行。
- 运行时间：完整 AutoSpec 在代码域耗时约 20 秒，具身域约 12 秒（含 ILP 求解）。
- 说明：ILP 求解器 ILASP 有 120 秒超时限制。

## 5. 实验数量与充分性

### 实验组数
- **2 个领域**（代码、具身），每个领域 4 个基线 + 1 个 AutoSpec = 5 个主要方法对比。
- **RQ2 收敛曲线**：每个领域一条曲线。
- **RQ3 泛化**：两个领域，初始/传入分数对比。
- **消融实验**：3 个变体（无 ILP、无多步编辑、无失败驱动分支），在代码域上报告 F1、迭代数、候选数。
- **用户研究**：10 名从业者，4 个场景，3 个维度评分（120 次比较）。

### 充分性与客观性
- 数据来源多样（RedCode、SafeAgentBench），覆盖不同风险类型。
- 基线设置合理，包括静态规则、神经网络、无 ILP 的 CEGIS、随机搜索，能够有说服力地归因 ILP 和 CEGIS 的贡献。
- 测试集与训练集分离，且报告了泛化差距。
- 消融实验独立验证每个组件的必要性。
- 用户研究采用配对比较和统计检验（Wilcoxon 检验）。
- **不足**：仅两个领域，未涵盖 Web Agent、Database Agent 等；代码域安全轨迹是人为生成的（可能 bias）；具身域轨迹数量较少（100条）；随机搜索基线采用固定预算而非自适应终止，可能有偏差。

## 6. 主要结论与发现

| 指标 | 代码域 | 具身域 |
|------|--------|--------|
| AutoSpec F1 | **0.980** | **0.933** |
| 对比 B1（专家规则） | 0.698 → +40.4% | 0.493 → +89.2% |
| 对比 B2（LLM分类器） | 0.813 → +20.5% | 0.881 → +5.9% |
| 对比 B3（CEGIS w/o ILP） | 0.480 → +104% | 0.667 → +39.9% |
| 对比 B4（随机搜索） | 0.692 → +41.6% | 0.493 → +89.2% |
| 假正例减少（相对专家规则） | 94% (31 FP → 2 FP) | – |
| 迭代次数 | 4 | 5 |
| ILP 引导 vs 启发式 F1 | **0.980 vs 0.203** | **0.933 vs 0.758** |
| 泛化到新任务 F1 | 0.957（初始化0.973，gap 1.6%） | 0.842（gap 4.7%） |

- ILP 引导是关键组件：移除后 F1 从 0.98 降至 0.20。
- 用户研究：AutoSpec 规则在可解释性、可操作性、信任度上显著优于 LLM 分类器（所有维度 p < 0.01）。
- 学习到的规则是 **人类可读、可审计** 的，且能保持精确率和召回率平衡。

## 7. 优点

1. **框架创新**：首次将安全规则维护形式化为 ILP 引导的 CEGIS 问题，充分利用已有专家规则和在线标注。
2. **高效搜索**：ILP 高效剪枝指数级搜索空间，收敛快（4-5 轮），无需 GPU，可在笔记本上运行。
3. **强适应性**：规则自动演化，弥补静态规则随环境漂移的缺陷；可应对模型更新、工具增加等变化。
4. **可解释性保持**：输出是符号逻辑规则，完全可审计，用户研究证明易修改、可信赖。
5. **泛化能力强**：在新任务上仅小幅度下降（gap 1.6%-4.7%），说明学到的是真实模式而非过拟合。
6. **消融实验系统**：清晰地分离了 ILP、多步编辑、失败驱动分支的贡献。

## 8. 不足与局限

1. **领域覆盖有限**：仅测试代码执行和具身智能体，未涉及 Web Agent、Database Agent 等更广泛场景。
2. **谓词库依赖专家设计**：若初始谓词库缺少关键谓词，则 ILP 无法学习到。论文承认这是本质限制，但将未覆盖的反例可作为扩展谓词库的信号。
3. **轨迹标注粒度**：人工标注在轨迹级别而非事件级别，可能忽略关键事件细节；ILP 编码已处理该问题，但可能对某些具身任务不够精确。
4. **随机搜索基线不充分**：采用固定评估预算而非自适应算法，可能低估了随机搜索的潜力。
5. **具身数据集规模小**：仅 100 条轨迹，且 70/30 划分后测试集仅 30 条，统计显著性有限。
6. **用户研究规模小**：10 名从业者，结果虽有统计显著，但样本量偏小，且未说明如何招募、是否有偏。
7. **未报告超参数敏感性**：如迭代次数上限、多样性权重等对结果的影响。
8. **计算效率**：虽然未用 GPU，但 ILP 求解可能在大规模反例集合下存在扩展性问题（论文中代码域仅 319 个候选）。

（完）
