---
title: A Markov Chain Approach to Preference Alignment
title_zh: 基于马尔可夫链的偏好对齐方法
authors: "Takuya Koriyama, Tengyuan Liang"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22652v1"
tags: ["query:ai-safety"]
score: 9.0
evidence: 基于马尔可夫链的偏好对齐方法，直接处理AI对齐问题
tldr: 针对生成模型对齐问题，现有方法如RLHF将成对偏好简化为标量奖励、NLHF通过KL正则化极小极大优化保留成对效用。本文提出MCHF方法，直接以成对偏好定义马尔可夫转移核，将模型输出分布迭代对齐至平稳分布。理论证明MCHF几何收敛，速率由偏好非传递性度量决定，且与NLHF的一阶近似在RLHF解附近等价。这统一了基于奖励、博弈论和马尔可夫链的对齐视角。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22652v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1235, \"height\": 468, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22652v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1181, \"height\": 555, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22652v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1526, \"height\": 509, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22652v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 597, \"height\": 423, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22652v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1687, \"height\": 482, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22652v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1654, \"height\": 499, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22652v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1717, \"height\": 1194, \"label\": \"Table\"}]"
motivation: 现有RLHF忽略偏好非传递性，NLHF计算复杂，需更直接利用成对偏好的对齐方法。
method: "定义马尔可夫核 P(x,dy)∝exp(U(x,y))μ_ref(dy)，从参考分布出发迭代采样实现对齐。"
result: MCHF几何收敛，速率由非传递性范数‖U‖_⊕控制；与NLHF在RLHF解附近一阶等价。
conclusion: MCHF为对齐提供新框架，统一奖励、博弈与马尔可夫方法，且收敛分析自适应当前偏好结构。
---

## 摘要
我们提出基于人类反馈的马尔可夫链（MCHF），这是一种从成对人类偏好中对齐生成模型的基础方法。与将比较简化为标量奖励的基于人类反馈的强化学习（RLHF）不同，以及通过KL正则化极小极大优化保留成对效用的基于人类反馈的纳什学习（NLHF）不同，MCHF直接使用成对偏好来定义模型输出上的转移机制。给定一个成对效用$U(x,y)$（量化人类对$y$相对于$x$的偏好）和一个参考概率分布$μ_{\mathsf{ref}}$，我们定义一个马尔可夫核$\mathsf{P}(x, dy)\propto \exp(U(x,y))μ_{\mathsf{ref}}(dy)$，并将从$μ_{\mathsf{ref}}$出发的马尔可夫链作为迭代对齐过程。我们证明MCHF以几何速度收敛到平稳分布，收敛速率由半范数$\|U\|_\oplus=\inf_{g,f\in L^\infty(μ_{\mathsf{ref}})}\|U-g\oplus f\|_\infty$控制，该半范数量化了成对效用的非传递结构。我们进一步证明，NLHF的镜像下降算法满足类似的结构自适应收敛保证。最后，通过扰动分析，我们证明当$\|U\|_\oplus$较小时，MCHF和NLHF在RLHF解附近一阶一致，从而为基于奖励、博弈论和马尔可夫的对齐方法提供了统一视角。特别地，对于收敛到MCHF/NLHF均衡的两个自然算法，我们证明MCHF和NLHF的第一步基于列和奖励$\hat{f}(y)=\int μ_{\mathsf{ref}}(dx) U(x, y)$恢复RLHF解，而从第二次迭代开始，两个算法都引入相同的残差$U-(-\hat f)\oplus \hat f$的线性泛函，该残差捕捉了成对效用$U$的非传递结构。

## Abstract
We propose Markov Chain from Human Feedback (MCHF), an elementary approach for aligning generative models from pairwise human preferences. Unlike Reinforcement Learning from Human Feedback (RLHF), which reduces comparisons to a scalar reward, and Nash Learning from Human Feedback (NLHF), which preserves pairwise utilities through a KL-regularized minimax optimization, MCHF uses pairwise preferences directly to define a transition mechanism over model outputs. Given a pairwise utility $U(x,y)$, which quantifies human preference for $y$ over $x$, and a reference probability distribution $μ_{\mathsf{ref}}$, we define a Markov kernel $\mathsf{P}(x, dy)\propto \exp(U(x,y))μ_{\mathsf{ref}}(dy)$, and take the Markov chain starting from $μ_{\mathsf{ref}}$ as an iterative alignment procedure. We show that MCHF converges geometrically fast to the stationary distribution, with a convergence rate governed by the seminorm $\|U\|_\oplus=\inf_{g,f\in L^\infty(μ_{\mathsf{ref}})}\|U-g\oplus f\|_\infty$, which quantifies the non-transitive structure of the pairwise utility. We further show that a mirror-descent algorithm for NLHF satisfies an analogous structure-adaptive convergence guarantee. Finally, through a perturbation analysis, we prove that when $\|U\|_\oplus$ is small, MCHF and NLHF agree up to first order around an RLHF solution, which yields a unified view of reward-based, game-theoretic, and Markovian approaches to alignment. In particular, for two natural algorithms that converge to the MCHF/NLHF equilibria, we show that the first step of MCHF and NLHF recovers the RLHF solution based on the column-sum reward $\hat{f}(y)=\int μ_{\mathsf{ref}}(dx) U(x, y)$, and starting from the second iteration, both algorithms incorporate the same linear functional of the residual $U-(-\hat f)\oplus \hat f$, which captures the non-transitive structure of the pairwise utility $U$.

---

## 论文详细总结（自动生成）

## 论文总结

### 1. 核心问题与整体含义  
生成的生成式模型对齐问题中，现有方法（RLHF、NLHF）各有局限：  
- RLHF 将成对偏好拟合为标量奖励，无法捕捉非传递性循环偏好。  
- NLHF 通过KL正则化极小极大优化保留成对效用，但计算复杂（需反复解KL优化）。  

本文提出 **MCHF（Markov Chain from Human Feedback）** ，直接利用成对偏好定义马尔可夫转移核，通过迭代将参考分布（预训练模型）对齐至平稳分布，从而统一了基于奖励、博弈论和马尔可夫链的对齐视角。核心洞察是：当偏好效用具有强加性（非传递性小）时，MCHF、NLHF 和 RLHF 的解在—阶近似下等价。

### 2. 方法论  
- **核心思想**：给定成对效用 $U(x,y)$（大表示更偏好 $y$）和参考分布 $\mu_{\mathsf{ref}}$，定义马尔可夫核  
  $$\mathsf{P}(x,dy) \propto \exp(U(x,y))\,\mu_{\mathsf{ref}}(dy)$$  
  从 $\mu_{\mathsf{ref}}$ 出发，重复应用 $\mu_{t+1} = \mu_t \mathsf{P}$，得到迭代对齐序列。  
- **关键技术细节**：  
  - 引入半范数 $\|U\|_\oplus = \inf_{g,f}\|U - (g\oplus f)\|_\infty$，其中 $g\oplus f(x,y)=g(x)+f(y)$。$\|U\|_\oplus$ 量化了效用的**非传递性**（非加性）。  
  - MCHF 的收敛率由 $\|U\|_\oplus$ 控制：$d_{\text{TV}}(\mu_{\mathsf{ref}}\mathsf{P}^t,\mu_{\text{MC}}) \le d_{\text{TV}}(\mu_{\mathsf{ref}},\mu_{\text{MC}})\cdot c^t$，其中 $c = \min(1-e^{-2\|U\|_\oplus}, \|U\|_\oplus)$，意味着算法自适应偏好结构。  
  - NLHF 的镜像下降算法（带局部KL正则化）同样具有类似自适应收敛保证（步长需选 $\eta \le 1/\|U\|_\oplus^2$）。  
  - 当 $\|U\|_\oplus \to 0$ 时，MCHF 与 NLHF 的一阶展开式相等于 RLHF 解（以列和奖励 $\hat f(y)=\int \mu_{\mathsf{ref}}(dx)U(x,y)$ 作为第一项），且第二迭代开始引入相同的非传递性修正。  

### 3. 实验设计  
- **数据集/场景**：未使用真实偏好数据，采用**合成数据**：  
  - 离散空间（$|X|=20$），参考分布为均匀分布；  
  - 效用矩阵由 $U(x,y)=\frac12\log\frac{\Phi(R(y)-R(x)+\frac12 E(x,y))}{1-\Phi(\cdots)}$ 生成，其中 $\Phi$ 是标准正态 CDF，$R\sim N(0,I_n)$，$E$ 为反对称随机矩阵。  
- **Benchmark**：对比 MCHF、NLHF、RLHF 的分布差异，以及一阶近似（Corollary 4.4）的准确性。  
- **对比方法**：主要是 RLHF（列和奖励法）以及 NLHF（极小极大解）。  

### 4. 资源与算力  
论文未提及任何 GPU 型号、训练时长、算力消耗等信息。全文为**理论分析为主**，数值模拟仅用于验证理论结论，不涉及大规模训练。  

### 5. 实验数量与充分性  
- **实验数量**：只进行了两组数值模拟：  
  1. 比较 MCHF、NLHF、RLHF 及一阶近似的分布（Figure 5）。  
  2. 分析推理时精炼的命中时间（Figure 6），展示第二阶导数的效果。  
- **充分性**：作为理论论文，模拟实验规模较小，但针对核心结论（一阶等价、收敛性）进行了定量验证，足以支持理论推导。然而，缺乏在真实 NLP 任务（如对话、翻译）上的评估，也未与实用对齐方法（DPO、PPO 等）比较。  

### 6. 主要结论与发现  
- MCHF 的马尔可夫链几何收敛，且收敛率由半范数 $\|U\|_\oplus$ 而不是平凡的 $\|U\|_\infty$ 控制，体现了结构自适应。  
- NLHF 的镜像下降算法（带局部KL项）在适当步长下也满足类似自适应收敛，收敛率优于先前依赖于 $\|U\|_\infty$ 的分析。  
- 当 $\|U\|_\oplus$ 很小时，MCHF 与 NLHF 的一阶项完全一致，且前两步迭代足以捕捉主要对齐效果：第一步等价于 RLHF（列和奖励），第二步引入非传递性修正。  
- 推理时精炼可以通过马尔可夫链的暂态动力学降低命中目标输出的期望步数（二阶导数条件）。  

### 7. 优点  
- **理论贡献扎实**：提出新颖的半范数 $\|U\|_\oplus$ 度量非传递性，并证明 MCHF 收敛率自适应偏好结构，这是对 RLHF/NLHF 理论的重要扩展。  
- **统一框架**：通过扰动分析，将奖励型、博弈型和马尔可夫型对齐方法在—阶近似下联系起来，揭示了它们的内在联系。  
- **洞察深刻**：证明前两步迭代已经包含主要对齐效果，为实际迭代式对齐提供了计算节约的指导（可能只需要两次更新）。  

### 8. 不足与局限  
- **实验覆盖严重不足**：只用了合成数据，规模很小（20个点），未在真实文本或多模态数据集上测试 MCHF 的实用效果。  
- **实现细节缺失**：论文承认实际采样马尔可夫核 $\mathsf{P}(x,dy)$ 需要条件生成技术，但并未提出或验证任何实用采样算法（如基于拒绝采样、扩散模型等），因此方法目前停留在理论层面。  
- **NLHF 镜像下降的步长依赖**：要达到自适应收敛率，步长需设为 $\eta \le 1/\|U\|_\oplus^2$，但 $\|U\|_\oplus$ 本身难以精确计算（虽然可用乘法常数估计）。且算法仍需局部KL项，实践中采样困难。  
- **偏差风险**：假定偏好效用 $U$ 能很好地反映人类偏好，且 $\mu_{\mathsf{ref}}$ 是事先给定的固定分布，未考虑分布偏移和噪声。  
- **无消融实验**：未分析不同 $\|U\|_\oplus$ 水平下收敛行为的差异（模拟中仅展示了一例）。  

（完）
