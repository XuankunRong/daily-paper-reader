---
title: Cryptographic certificates of validity for trustworthy AI
title_zh: 可信人工智能的密码有效性证书
authors: Murdoch J. Gabbay
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23768v1"
tags: ["query:ai-safety"]
score: 9.0
evidence: 可信AI的加密有效性证书
tldr: 针对可信AI中agent行为的可验证性问题，提出加密有效性证书方法。将正确性或策略条件形式化为逻辑谓词，编译为多项式约束的见证检查问题，利用简洁加密证明系统生成独立可验证的证明。该方法无需信任agent或重新执行计算，在形式验证与加密认证之间取得平衡，为agent治理提供新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23768v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 853, \"height\": 172, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23768v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 818, \"height\": 579, \"label\": \"Table\"}]"
motivation: 现有可信AI方法要么依赖源代码形式验证难以扩展，要么缺乏可独立验证的保证；需要一种中间方案。
method: 将agent行为策略编码为逻辑谓词，转换为多项式约束系统，使用succinct proof或零知识证明生成证书。
result: 证明可独立验证，无需信任agent或重算，支持零知识保护隐私，降低验证成本。
conclusion: 提出加密有效性证书作为可信AI的基础设施，结合形式验证与加密认证，促进agent治理与审计。
---

## 摘要
我们提出了面向代理式人工智能系统的密码有效性证书。其核心思想是将正确性或策略条件形式化为逻辑谓词，将该谓词编译为多项式约束上的见证检验问题，并使用简洁的密码证明系统（并可选择零知识）来证明该条件成立。这为源代码的形式化验证与密码认证提供了一种中间方案。代理的行为可以附带一个独立可检查的证明，表明其满足约定的形式策略，而无需验证者信任该代理或重新执行计算。我们高层面概述了该方法，给出了核心数学转换，将该提案与证明携带代码、zkVM、形式化方法和代理治理联系起来，并指出了完整实现必须解决的规范、审计和部署问题。

## Abstract
We propose cryptographic certificates of validity for agentic AI systems. The core idea is to formally specify a correctness or policy condition as a logical predicate, compile this predicate to a witness-checking problem over polynomial constraints, and use a succinct cryptographic proof system (and optionally zero-knowledge) to certify that the condition holds.   This offers a middle ground between formal verification of source code, and cryptographic authentication. An agent's action can be accompanied by an independently checkable proof that it satisfies an agreed formal policy, without requiring the verifier to trust the agent or to re-execute computation. We outline the approach at a high level, give the core mathematical translation, relate the proposal to proof-carrying code, zkVMs, formal methods, and agent governance, and note the specification, auditing, and deployment questions that a full implementation must answer.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：代理式AI系统（agentic AI）正在自主执行行动（如订票、审批发票、部署代码、物理世界操作），然而现有的信任机制（密码签名、日志、监控）只能证明来源或事后复盘，无法在行动前证明该行动符合正确性、安全性或合规性条件。因此，需要一种**可独立验证的、事先的、密码学保证的正确性证明**。
- **研究动机**：传统形式化验证（如Hoare逻辑、证明携带代码）虽然可以提供机器可检查的保证，但通常要求访问源代码或重新执行计算，不适合黑盒、异构、可能拜占庭的AI代理。密码认证（如数字签名）只能认证身份，不能认证行为正确性。本文旨在填补形式验证与密码认证之间的空白，提出一种“中间方案”：**对代理的行动附加简洁的密码有效性证书**，验证者无需信任代理、无需重新计算，仅通过检查证书即可确信行为符合约定策略。
- **整体含义**：构建可信AI的基础设施，使得任何代理（即使是未受信任的第三方）只要携带有效证书，其行动即可被接受。这为AI治理、审计、合规提供了新范式，尤其适用于自主行动频率高、后果严重的场景。

## 2. 论文提出的方法论

### 核心思想
1. **形式化策略**：将正确性、安全性或合规条件写成一阶逻辑谓词（如图1中的语法）。
2. **算术化**：将该谓词编译（compile）成关于多项式约束的见证检查问题（witness-checking problem），利用多项式语义（图2）将逻辑真值映射为多项式在整数点上的值——0表示有效/真，正数表示无效/假。
3. **生成密码证明**：使用简洁的密码证明系统（如zk-SNARKs、Halo2、Zinc等）生成一个简洁证明（certificate），证明存在满足该多项式约束的见证数据。可选的零知识设置下，验证者无需知道见证细节。
4. **验证**：接收方仅需使用公开验证密钥检查证书，无需信任代理或重新执行任何计算。

### 关键技术细节
- **逻辑语法**（图1）：定义了项（项包括变量、整数常数、加/乘、函数符号`Ci(t)`、长度`len(C)`、反身化`reify(ϕ)`）和谓词（等式`t===t`、合取`∧∧∧`、析取`∨∨∨`、全称量化`∀∀∀CX.ϕ`、范围检查`Ci <<< t`、`t <<< Ci`）。
- **多项式语义**（图2）：对所有语法结构给出到有理系数多项式`Q[X]`的映射。例如：
  - `[[t===t']]ς = ([[t]]ς - [[t']]ς)²`，等式成立当且仅当差平方为零。
  - `[[ϕ ∧∧∧ ψ]]ς = [[ϕ]]ς + [[ψ]]ς`，合取成立当且仅当和为零（因为两项非负）。
  - `[[ϕ ∨∨∨ ψ]]ς = [[ϕ]]ς * [[ψ]]ς`，析取成立当且仅当乘积为零（至少一项为零）。
  - `[[∀∀∀CX.ϕ]]ς` 为对列索引求和，每个索引处求值`[[ϕ]]ς(x)`。
  - 范围检查`Ci <<< t` 返回0当且仅当t严格小于Ci的每一列对应元素，否则返回1。
- **核心定理**（Theorem 2.9）：证明了该语义与标准逻辑语义的**可靠性与完全性**（soundness and completeness），即多项式结果为零当且仅当逻辑谓词在对应解释下成立。
- **算术化例子**（第3节）：
  - 用`χm`表示矩阵的第二行是第一行的负值（模拟`λx.9x`）。
  - 用`χa`表示第三行为前两行之和（模拟加法`λx,x'.x+x'`）。
  - 用`χpow`（图4）编码指数运算`a^b`，通过递归定义和矩阵指针实现，展示了如何用证书结构证明递归计算正确性（类似于证明携带代码中的推导树指针）。
- **证书结构**（第4节）：抽象定义为 `Cert = (policyID, action, pub, vk, paramsHash, π)`，其中π是简洁证明，证明存在见证w使得`R_{ϕ,action}(pub,w)=0`。策略作者编写谓词ϕ，编译器将其转换为代数关系R，代理生成π，验证者检查π。

### 算法流程（文字说明）
1. 系统管理员固定一个策略谓词ϕ（如“所有转账金额不超过限额”）。
2. 编译器将ϕ和公开行动数据`action`编译成代数关系`R_{ϕ,action}`。
3. 代理或其关联证明器利用秘密见证w（如内部状态、历史数据）生成简洁证明π。
4. 接收方使用公开验证密钥vk检查π，若通过则接受行动，否则拒绝。

## 3. 实验设计

- **本文为理论/提案性论文**，没有进行实验验证（没有数据集、benchmark、对比方法）。作者在第4节末提到了“规划路线图”（roadmap），包括如何编写审计策略、如何测试编译器、如何选择证明后端等，但这些均属未来工作。
- **仅有的“例子”**：第3节给出了三个逻辑谓词的构造（χm、χa、χp）以及指数运算χpow的矩阵编码演示，用于说明表达能力和证书结构，并非实验验证。

## 4. 资源与算力

- **未提及任何计算资源**（GPU型号、数量、训练时长等）。论文属于理论提案，未进行实际训练或证明系统实现。

## 5. 实验数量与充分性

- **实验数量为0**。本文未进行任何实验（如不同数据集、消融实验、对比实验等）。
- **充分性评估**：作为概念性工作，缺乏实验验证是合理的，但也是主要局限之一。读者无法评估该方案在实际中的计算开销、证明大小、验证时间等关键性能指标。

## 6. 论文的主要结论与发现

- **主要结论**：提出了一种将一阶逻辑谓词直接编译为多项式约束，并用简洁密码证明系统生成有效性证书的方法，作为可信AI中“事先验证行动正确性”的通用框架。
- **核心发现**：
  - 逻辑真值（0代表真，正数代表假）与结式（多项式零化）完全对应，使得可靠性和完全性成立。
  - 通过矩阵和指针机制，可以编码递归计算（如指数运算），从而支持图灵完备的计算验证。
  - 该方法在理论上架起了形式化规范与密码认证之间的桥梁，特别适用于代理式AI的自治行动验证，但证书仅证明“谓词被满足”，不证明谓词本身是正确的（即规范选择、编译器正确性、密码系统参数治理等问题仍需通过治理流程解决）。

## 7. 优点

- **新颖性**：将形式验证的精确性与密码证明的简洁性有机结合，提出了一种新的信任机制——不依赖来源信任，而是依赖可检查的密码证据。
- **通用性**：方法独立于具体应用，可应用于任何需要提供正确性证据的场景（如金融交易、机器人控制、智能合约）。
- **可组合性**：通过逻辑连接词和递归编码，表达能力强大，可以编码各种约束。
- **零知识可选**：支持零知识证明，保护代理内部隐私。
- **减少信任假设**：验证者无需信任代理、无需查看内部实现或重新计算，只需检查一个简洁证明。
- **数学严谨性**：提供了完整的一阶逻辑到多项式的算术化，并证明了可靠性和完全性。

## 8. 不足与局限

- **缺乏实现与实验**：论文纯理论，未给出具体实现、性能基准或实际部署案例，无法评估可行性、证明大小、验证时间等关键指标。
- **编译器信任**：从谓词到代数关系的编译器必须被信任或经过形式化验证，否则证书可能无效。论文未讨论编译器验证问题。
- **规范缺口**：证书仅证明“谓词被满足”，但谓词本身可能不正确（例如漏洞或未覆盖安全需求），这需要治理和审计流程来弥合。
- **应用限制**：不是所有代理行为都适合或需要事前证书（例如实时性要求极高的低风险行动）。论文未讨论适用性边界。
- **密码学假设**：依赖底层密码证明系统的安全假设，存在理论上的攻击风险（如碰撞、可信设置问题），文中未深入讨论。
- **没有消融实验**：未分析不同算术化策略（如乘法复杂度、约束数量）对性能的影响。
- **缺乏与其他方法的定量比较**：未与zkVM（如Cairo、RISC0）或证明携带代码（PCC）在计算开销、证明大小等方面进行对比。

（完）
