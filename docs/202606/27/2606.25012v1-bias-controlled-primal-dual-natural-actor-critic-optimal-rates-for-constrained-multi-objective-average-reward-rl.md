---
title: "Bias-Controlled Primal-Dual Natural Actor-Critic: Optimal Rates for Constrained Multi-Objective Average-Reward RL"
title_zh: 偏差控制的原-对偶自然Actor-Critic：约束多目标平均奖励强化学习的最优速率
authors: "Ankur Naskar, Swetha Ganesh, Vaneet Aggarwal"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25012v1"
tags: ["query:agent-safety"]
score: 9.0
evidence: 带安全约束的多目标强化学习
tldr: 在无限时域平均报酬强化学习中，多目标优化与安全约束问题因凹标量化方法引入非线性偏差而复杂。本文提出基于多层蒙特卡洛（MLMC）的原始-对偶自然Actor-Critic算法，有效控制标量化目标、约束评估和actor-critic估计中的偏差，无需混合时间知识。该算法实现最优全局收敛和约束违反率O~(1/√T)。这是首次在平均报酬设置下为凹标量多目标RL建立最优收敛，无论是否含约束，且无需混合时间信息。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25012v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1185, \"height\": 425, \"label\": \"Table\"}]"
motivation: 凹标量化中非线性函数导致梯度估计偏差，影响收敛和约束满足，需控制偏差以实现最优速率。
method: 利用MLMC控制偏差，设计原始-对偶自然Actor-Critic算法，无需已知混合时间。
result: 达到最优收敛速率O~(1/√T)，约束违反率同阶。
conclusion: 首个在平均报酬多目标RL中实现最优收敛的结果，消除了混合时间依赖。
---

## 摘要
在无限时域平均奖励设定下，许多强化学习问题需要优化多个相互冲突的目标，同时满足多个安全约束。一种常见方法是凹标量化，其中智能体在满足标量化约束$ g(J^π_{c_1}, \ldots, J^π_{c_N}) \ge 0 $的条件下最大化效用$ f(J^π_{r_1}, \ldots, J^π_{r_M}) $，其中$J^π_{r_m}$和$J^π_{c_n}$分别表示策略$π$下的平均奖励和代价。然而，由于梯度必须使用$J^π$的有噪估计进行评估，且$ \mathbb{E}[\partial f(J^π)] \neq \partial f(\mathbb{E}[J^π])$，$f$和$g$的非线性会在策略梯度和Actor-Critic方法中引入偏差，这种偏差会通过原始更新和对偶更新传播。我们提出了一种基于MLMC的原始-对偶自然Actor-Critic算法，用于平均奖励MDP，该算法无需混合时间信息即可控制标量化目标、约束评估和Actor-Critic估计中的偏差。我们证明该算法实现了最优全局收敛率和约束违反率$ \tilde{O}(1/\sqrt{T}) $。据我们所知，这是首个在平均奖励设定下（无论是否带有约束）为凹标量化多目标强化学习建立最优收敛性的结果，并且即使在无标量化的情况下，也是首个无需混合时间信息即达到该结果的工作。

## Abstract
Many reinforcement learning (RL) problems in the infinite-horizon average-reward setting require optimizing multiple conflicting objectives while satisfying multiple safety constraints. A common approach is concave scalarization, where the agent maximizes a utility $ f(J^π_{r_1}, \ldots, J^π_{r_M}) $ subject to a scalarized constraint $ g(J^π_{c_1}, \ldots, J^π_{c_N}) \ge 0 $, where $J^π_{r_m}$ and $J^π_{c_n}$ denote the average-reward and cost under policy $π$. However, the nonlinearity of $f$ and $g$ introduces bias in policy-gradient and actor-critic methods, since gradients must be evaluated using noisy estimates of $J^π,$ and $ \mathbb{E}[\partial f(J^π)] \neq \partial f(\mathbb{E}[J^π]),$ and this bias propagates through both primal and dual updates. We propose an MLMC-based primal-dual Natural Actor-Critic algorithm for average-reward MDPs that controls bias in scalarized objectives, constraint evaluation, and actor-critic estimation without requiring mixing-time knowledge. We show that the algorithm achieves optimal global convergence and constraint-violation rates of $ \tilde{O}(1/\sqrt{T}) $. To our knowledge, this is the first result establishing optimal convergence for concave scalarized multi-objective RL in the average-reward setting, both with and without constraints, and the first to do so without mixing-time information even in the absence of scalarization.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在无限时域平均奖励（average-reward）设置下，强化学习（RL）需要同时优化多个冲突目标并满足安全约束。凹标量化（concave scalarization）是一种常见方法，但非线性函数 $f$ 和 $g$ 会引入梯度估计偏差（因为 $\mathbb{E}[\partial f(X)] \neq \partial f(\mathbb{E}[X])$），该偏差会通过原始-对偶（primal-dual）更新传播，使得收敛分析变得困难。
- **研究空白**：现有工作要么只处理参数化策略（parametrized policies）但无多目标标量化，要么处理凹标量化多目标但依赖模型或表格表示（不适用于大状态/动作空间），且均需已知混合时间（mixing time）。尚无工作在平均奖励设置下同时实现参数化策略、凹标量化、约束处理以及**无需混合时间知识**的最优收敛保证。
- **整体意义**：本文首次建立了带约束的凹标量化多目标平均奖励强化学习在参数化策略下的有限时间收敛性，证明了 $\tilde{O}(1/\sqrt{T})$ 的最优速率（modulo 参数化误差和函数逼近误差），且无需预先知道混合时间，填补了理论研究的重要空白。

## 2. 论文提出的方法论：核心思想、关键技术、公式或算法流程

- **核心思想**：利用多层蒙特卡洛（MLMC）估计联合控制偏差，设计一个原始-对偶自然 Actor-Critic 算法，使标量化目标、约束评估和 Actor-Critic 估计中的偏差均可控，从而在不依赖混合时间的前提下达到最优收敛。

- **关键技术与公式**：
  1. **问题建模**：求解约束优化问题 $\max_\theta f(J_\theta^r)$ s.t. $g(J_\theta^c) \ge 0$，转化为鞍点问题 $\max_{\theta \in \Theta} \min_{\lambda \ge 0} L(\theta,\lambda) = f(J_\theta^r) + \lambda g(J_\theta^c)$。
  2. **参数更新规则**（算法第9式）：  
     $\theta_{k+1} = \theta_k + \alpha w_k$  
     $\lambda_{k+1} = \Pi_{[0,2/\delta]}(\lambda_k - \beta g_k)$  
     其中 $w_k$ 和 $g_k$ 分别是自然策略梯度向量和约束代价的估计。
  3. **Critic估计**：使用线性随机递归（11式）估计值函数和平均奖励，借助特征函数 $\phi(s)$ 和参数 $\xi_{u,k}$。
  4. **NPG估计**：通过 SGD 更新自然梯度方向（13式），梯度定义为（14式）涉及 $\hat{F}_k^{kh}$ 和 $\hat{b}_k^{kh}$。
  5. **MLMC估计**：对所有需要采样的估计量（$\hat{q}, \hat{P}, \hat{F}, \hat{b}, g, \partial_m f, \partial_n g$）均采用 MLMC 构造：  
     $X_{\text{MLMC}} = X_1 + \mathbf{1}_{2Q \le T_{\max}} \cdot 2Q (X_{2Q} - X_{2^{Q-1}})$，其中 $Q \sim \text{Geom}(1/2)$。  
     这样做使得偏差与用 $T_{\max}$ 个样本的简单估计同阶，但期望样本数仅为 $O(\log_2 T_{\max})$，极大降低样本复杂度，且无需混合时间信息。

- **算法流程**（见附录 Algorithm 1）：外层循环 $K$ 次，每次：
  - 用 MLMC 估计标量化梯度 $\partial_m f(\hat{J}^r)$、$\partial_n g(\hat{J}^c)$ 和约束代价 $g_k$；
  - 内层循环 $H$ 次，用 MLMC 估计 critic 参数 $\xi_{u,k}$（通过线性递归）；
  - 内层循环 $H$ 次，用 MLMC 估计 NPG 参数 $w_k$（通过 SGD）；
  - 更新策略参数 $\theta_k$ 和对偶变量 $\lambda_k$（带投影）。

## 3. 实验设计：使用了哪些数据集/场景，benchmark，对比方法

**论文为纯理论分析工作，未包含任何实验验证。** 文中仅通过表格（Table 1）与现有方法进行理论对比：

- **对比方法**（理论上）：
  - 平均奖励 Actor-Critic（无约束，无标量化）[14]
  - 平均奖励 Primal-Dual AC（有约束，无标量化，需混合时间）[33]
  - 平均奖励 Model-based（无参数化策略，无约束，有标量化）[2,1]
  - 平均奖励 Model-based PD（有约束，有标量化，无参数化）[3]
  - 折扣奖励 Policy Gradient（有参数化，无约束，有标量化）[13]
- 论文声称其方法在设置（平均奖励、参数化策略、带约束、凹标量化、无混合时间知识）上首次达到最优速率 $\tilde{O}(1/\sqrt{T})$。

## 4. 资源与算力

论文未提及任何实验资源或算力需求（无实验）。作为理论工作，不涉及 GPU 型号、数量、训练时长等。

## 5. 实验数量与充分性

论文不包含实验，因此无法评价实验数量或充分性。所有结论均基于数学证明（定理、引理等），并附有详细的证明概要（附录给出完整证明）。由于缺少实证验证，结论在理论上的充分性依赖于假设（如 ergodicity、Slater condition、smoothness、特征矩阵正定等）是否在真实任务中可满足。

## 6. 论文的主要结论与发现

- 提出了 **MO-PDNAC**（Multi-Objective Primal-Dual Natural Actor-Critic）算法，能够在平均奖励 MDP 下处理凹标量化多目标约束问题，且无需混合时间知识。
- 在满足一系列标准假设（Assumptions 1–8）下，得到了**最优**全局收敛率和约束违反率：$\tilde{O}(1/\sqrt{T})$（见 Theorem 1）。
- 这是**首次**在平均奖励设置下，为参数化策略的凹标量化多目标 RL（无论是否带约束）建立有限时间最优收敛保证。
- 即使在无标量化的单目标情况下，也改进了先前工作 [33] 对混合时间的依赖问题，达到了同样最优速率。
- 证明了 MLMC 技术可以同时控制标量化偏差、Actor-Critic 估计偏差和对偶更新偏差，且不会影响收敛率。

## 7. 优点

- **理论创新性强**：首次解决平均奖励下凹标量化多目标 RL 的偏差耦合挑战，获得最优收敛速率。
- **方法完善**：MLMC 设计巧妙，既能消除混合时间依赖，又不增加渐近复杂度（使用 $O(\log_2 T_{\max})$ 期望样本达到 $T_{\max}$ 样本的偏差水平）。
- **适用范围广**：不仅适用于带约束的多目标情形，也覆盖无标量化的单目标和无约束情况。
- **证明框架严谨**：分步骤（Lagrange error → NPG 误差 → Critic 误差 → 标量化误差 → 最终 bound）递进，逻辑清晰。
- **假设相对标准**：多数假设（如 ergodicity、Lipschitz 平滑、特征矩阵正定、函数逼近误差）在强化学习理论文献中常见，并非过度理想化。

## 8. 不足与局限

- **缺少实验验证**：论文未在仿真或真实环境中进行任何算法实验，因此无法评估算法在实际问题中的收敛行为、稳健性以及与现有方法的实证差距。实际中假设（如精确估计、混合时间均匀上界等）可能难以满足，数值表现有待检验。
- **假设较强**：例如 Assumption 5（Fisher 信息矩阵正定）要求在参数空间所有点严格正曲率，这在深度策略网络中不一定成立；Assumption 7（特征矩阵正定）可能需精心设计特征；Assumption 6（参数化表示误差 $\varepsilon_{\text{bias}}$）隐含参数化足够丰富。
- **算法效率**：尽管理论样本复杂度最优，但每轮外层迭代需执行多个内循环（critic 和 NPG 各 $H$ 步），且每步都涉及多条超长轨迹（最长 $T_{\max}$）的 MLMC 采样，实际计算开销可能较高。
- **仅关注全局收敛率**：未分析算法对初始参数、学习率等超参数的敏感性，也未给出有限样本下的具体常数估计。
- **未考虑 non-stationary 或非 ergodic 环境**：假设 MDP 是遍历的，且混合时间 $\tau_{\text{mix}}$ 有界，这在许多真实系统中可能不成立。

（完）
