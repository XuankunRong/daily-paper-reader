---
title: "AutoSpec: Safety Rule Evolution for LLM Agents via Inductive Logic Programming"
title_zh: AutoSpec：基于归纳逻辑编程的LLM智能体安全规则演化
authors: "Pingchuan Ma, Zhaoyu Wang, Zimo Ji, Yuguang Zhou, Zhantong Xue, Zongjie Li, Shuai Wang, Xiaoqin Zhang"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24245v1"
tags: ["query:agent-safety"]
score: 9.0
evidence: 通过归纳逻辑编程为LLM智能体演化安全规则
tldr: "大型语言模型代理在自动化任务时面临安全风险，现有规则要么过于保守要么过于宽松。AutoSpec采用反例引导的归纳综合与归纳逻辑编程，自动从用户标注中演化可解释的安全规则。在代码执行和具身代理任务上，规则F1达到0.98和0.93，假正例降低94%，仅需4-5次迭代。该方法生成的规则可读、可审计，并泛化到未见场景。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24245v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 629, \"height\": 474, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 745, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 867, \"height\": 365, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 878, \"height\": 667, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24245v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 661, \"height\": 368, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24245v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 838, \"height\": 296, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 559, \"height\": 285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 810, \"height\": 566, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24245v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 834, \"height\": 256, \"label\": \"Table\"}]"
motivation: 现有LLM代理安全规则面临解释性与准确率的权衡，手工规则易误判，神经分类器缺乏可解释性。
method: AutoSpec通过反例引导的归纳综合与归纳逻辑编程，自动从标注轨迹中学习规则编辑，剪枝搜索空间。
result: "在291条轨迹上，规则F1分别达0.98和0.93，假正例减少94%，收敛于4-5次迭代，F1比启发式方法高4.8倍。"
conclusion: AutoSpec生成可读、可审计的安全规则，有效平衡精度与召回，并泛化到未见场景。
---

## 摘要
大型语言模型（LLM）智能体通过将语言模型与外部工具和环境相结合，日益自动化地完成复杂任务。然而，它们的自主性带来了显著的安全风险：智能体可能执行破坏性命令、泄露敏感数据或违反领域约束。现有的安全方法面临一个基本权衡：手工制定的规则可解释但脆弱，过于保守的规则会阻碍安全操作（高误报率），而宽松的规则会遗漏不安全行为（高漏报率）。神经分类器缺乏安全关键部署所需的可解释性。我们提出AutoSpec，一个通过反例引导的归纳综合（CEGIS）并辅以归纳逻辑编程（ILP）自动从用户安全/不安全标注中演化已部署的专家设计安全规则的框架。从专家规则和带标注的轨迹流开始，AutoSpec迭代评估规则，挖掘误报和漏报反例，使用ILP学习哪些谓词能够区分它们，生成候选规则编辑，并验证候选以选择最佳修订。关键洞察在于ILP高效地识别那些在漏报中频繁出现但在误报中很少出现（或反之）的谓词，从而大幅剪枝规则编辑的指数级搜索空间。这一过程持续直至收敛，产生在精确率和召回率之间平衡的可解释规则。我们在涵盖代码执行和具身智能体领域的291个执行轨迹上评估AutoSpec。AutoSpec将规则F1分数提升至两个领域的0.98和0.93，实现了高达94%的误报率降低同时保持高召回率，并在4-5次迭代内收敛。基于ILP的方法比启发式CEGIS实现了高达4.8倍的F1提升。学习到的规则可读、可审计，并能泛化到未见过的场景。

## Abstract
Large language model (LLM) agents increasingly automate complex tasks by integrating language models with external tools and environments. However, their autonomy poses significant safety risks: agents may execute destructive commands, leak sensitive data, or violate domain constraints. Existing safety approaches face a fundamental tradeoff: hand-crafted rules are interpretable but brittle, with overly conservative rules blocking safe operations (high false positives) while permissive rules miss unsafe behaviors (high false negatives). Neural classifiers lack the interpretability required for safety-critical deployments.   We present AutoSpec, a framework that automatically evolves deployed expert-designed safety rules from user safe/unsafe annotations through counterexample-guided inductive synthesis (CEGIS) guided by inductive logic programming (ILP). Starting from the expert rules and a stream of annotated traces, AutoSpec iteratively evaluates rules, mines false-positive and false-negative counterexamples, uses ILP to learn which predicates discriminate them, generates candidate rule edits, and verifies candidates to select the best revision. The key insight is that ILP efficiently identifies predicates that appear frequently in false negatives but rarely in false positives (or vice versa), dramatically pruning the exponential search space of rule edits. This continues until convergence, producing interpretable rules that balance precision and recall.   We evaluate AutoSpec on 291 execution traces spanning code execution and embodied agent domains. AutoSpec raises rule F1 to 0.98 and 0.93 across the two domains, achieving up to 94% false positive reduction while maintaining high recall, and converges within 4-5 iterations. The ILP-guided approach achieves up to 4.8x higher F1 than heuristic CEGIS. The learned rules are human-readable, auditable, and generalize to unseen scenarios.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化、深入且客观的中文总结。

---

### 论文中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）

-   **核心问题**：大型语言模型（LLM）智能体在自动化任务时，其自主性带来了巨大的安全风险。现有的安全方法面临一个根本性困境：
    -   **手工规则**可解释性强，但静态、脆弱，难以平衡精确率（高误报）和召回率（高漏报），需要专家持续维护。
    -   **神经分类器**自适应性强，但作为“黑箱”模型，缺乏可解释性和可审计性，不适合安全关键部署。
-   **研究背景**：部署的LLM智能体会随着时间推移不断演化（模型更新、集成新工具、提示词修改），导致初始的安全规则逐渐失效。同时，智能体在执行过程中会产生大量的执行轨迹和用户安全/不安全标注。
-   **整体含义**：本文旨在将安全规则的维护问题从一个需要专家手动、一次性的创作任务，转变为一个由标注驱动的、持续的、自动化的规则演化问题。即如何利用智能体运行时产生的反馈信号，来自动地、可解释地改进已有的专家安全规则。

#### 2. 论文提出的方法论：核心思想、关键技术细节

-   **核心思想**：将安全规则的演化问题形式化为一个**反例引导的归纳综合（CEGIS）** 问题。用户对智能体行为的标注（安全/不安全）即是“反例”（Counterexamples），规则编辑过程就是根据这些反例迭代地进行“综合”（Synthesis）。为了高效地搜索巨大的规则编辑空间，引入**归纳逻辑编程（ILP）** 来引导搜索，快速找出最能区分反例的谓词（Predicates）。

-   **关键技术细节（AutoSpec框架）**：
    1.  **输入**：初始专家规则 (R0)、带标注的执行轨迹数据集 (DL)、谓词库 (P)。
    2.  **输出**：经过演化、性能提升的新规则集 (R*)。
    3.  **核心算法流程（基于CEGIS的4阶段迭代循环）**：
        -   **阶段1：反例挖掘**：在标注数据集上评估当前规则集R，找出**假阳性（FP）** 和**假阴性（FN）** 轨迹。
        -   **阶段2：ILP引导的谓词学习**：将FP和FN作为训练数据，使用ILP求解器（如ILASP）学习一个能区分它们的逻辑假设（Hypothesis H）。该假设是谓词的简单组合（如 `rd_cred ∧ ext_host`）。H中出现的谓词即为最具有辨别力的“建议谓词”。
        -   **阶段3：候选规则生成与选择**：基于ILP给出的建议和4种基本编辑操作符，生成一系列候选规则集。编辑操作符包括：
            -   `AddConjunct` / `AddException` (收紧规则，用于减少FP)
            -   `AddDisjunct` / `Relax` (放宽规则，用于减少FN)
            -   使用`混合修复链`和`失败驱动分支`策略生成更复杂的候选。最后，根据一个多层级评分函数（优先F1，其次精确率、未解决反例数、规则复杂度）选择最优候选。
        -   **阶段4：验证与迭代**：检查最优候选是否比当前规则集R更好。如果是，则更新R并开始下一轮；如果否或达到目标分数/迭代次数上限，则停止。

#### 3. 实验设计：数据集、基准与对比方法

-   **数据集**：
    1.  **代码执行域**：191条轨迹。91条不安全轨迹来自`RedCode-Exec`基准（涵盖25种风险类别），100条安全轨迹由`GPT-5.1-Codex`生成（对应安全操作）。
    2.  **具身智能体域**：100条轨迹，来自`SafeAgentBench`基准。模拟家庭机器人在AI2-THOR环境中执行任务，包括安全和危险行为。
-   **评估指标**：在轨迹粒度上报告精确率（Precision）、召回率（Recall）和F1分数。同时关注收敛迭代次数、规则复杂度和候选生成效率。
-   **对比方法**：
    -   **B1：专家设计规则**：行业中广泛使用的`AgentSpec`规则，也是AutoSpec的**初始规则**。
    -   **B2：LLM分类器**：使用与AutoSpec相同的标注数据作为少样本示例，让LLM直接判断轨迹安全性。
    -   **B3：无ILP的CEGIS**：AutoSpec的消融版本，不使用ILP来学习/选择谓词，而是使用简单的覆盖统计进行启发式排名。
    -   **B4：随机搜索**：随机采样规则编辑，在固定预算内选择最佳结果。

#### 4. 资源与算力

-   **明确说明**：论文明确指出所有实验运行在一台**MacBook Pro (M2 Max, 32 GB RAM)** 上。使用了`ILASP v4.4.1`作为ILP求解器。
-   **未明确说明**：论文**没有提及**使用GPU。对于ILP求解和候选规则评估，在给定的数据集规模和谓词库下，在MacBook Pro上运行是可行的。因此，该方法主要依赖CPU资源。

#### 5. 实验数量与充分性

-   **实验数量**：论文进行了多组实验以回答三个研究问题（RQ1-RQ3），并包含消融实验：
    -   **RQ1（有效性）**：在两个数据集上对比了AutoSpec与4个基线方法的性能。
    -   **RQ2（搜索效率）**：展示了在两个数据集上的收敛曲线，并对有无ILP指导的谓词选择进行了对比。
    -   **RQ3（迁移与可用性）**：进行了70%/30%的初始/新任务划分，测试规则泛化能力。并进行了用户研究（10名从业者，4个场景，两个方法的对比）。
    -   **消融实验**：在代码执行域上，分别去除了**ILP指导、失败驱动分支、多步编辑**三个关键组件，进行了3组消融实验。
-   **充分性与公平性**：
    -   **优点**：实验覆盖了**两个完全不同的领域**（代码与机器人），验证了方法的通用性。对比基线选择合理，B1（专家规则）、B2（LLM分类器）B3（消融）、B4（随机搜索）共同说明了AutoSpec各个组件的贡献。消融实验设计清晰，量化了每个组件的价值。用户研究扩展了评估维度，涵盖可用性和可信度。
    -   **潜在不足**：每个领域的数据集规模相对有限（不到200条），可能无法完全代表真实世界中复杂多样的场景。代码域的安全轨迹是已有的基准（RedCode），但有91条，安全轨迹是生成的，可能存在数据偏差。用户研究的样本量较小（10人），其结论的代表性有待更大规模研究验证。实验只比较了B3（无ILP的CEGIS），但没有比较其他符号规则学习方法（如RIPPER, CN2）。

#### 6. 论文的主要结论与发现

-   **有效性**：AutoSpec在代码执行域F1达到**0.980**，在具身智能体域F1达到**0.933**，均显著优于所有基线方法，实现了精确率和召回率的良好平衡。
-   **搜索效率**：ILP引导的谓词搜索是关键创新。与无ILP的启发式CEGIS相比，F1分数提升了**4.8倍**。规则在**4-5次**迭代内即可收敛。
-   **泛化能力**：从70%的初始任务学习到的规则，在30%的未见新任务上，F1分数仅下降**1.6%**（代码域）和**4.7%**（具身域），表现出良好的泛化性能。
-   **可解释性与可用性**：用户研究显示，相比LLM分类器，从业者认为AutoSpec生成的规则在**可解释性、可操作性和信任度**三个维度上都显著更好，认为其更适合生产部署。
-   **关键贡献总结**：AutoSpec能够将人工标注自动转化为性能优异的、可读的、可审计的、可部署的安全规则，赋予了符号规则层类似于学习模型的适应性，同时保持了其核心优势。

#### 7. 优点：方法或实验设计上的亮点

-   **方法上的创新性**：将ILP与CEGIS相结合来演化安全规则，是一个新颖且优雅的方案。ILP高效地剪枝了指数级的搜索空间，使得从有限的反例中学习变得可行。
-   **问题定义的准确性**：将安全规则维护明确定义为“从已有专家规则开始演化”，而不是从零生成，这更贴近工程实践，也使得方法更加实用和可信。
-   **实验设计的说服力**：实验设计层次分明，从**有效性**（总体效果）、**效率**（为什么有效）到**泛化与可用性**（实际价值），层层深入。特别是用户研究，从实践者视角验证了方法的“软价值”，非常有说服力。
-   **代码开源**：提供了开源代码，增强了结果的可复现性和方法的可用性。

#### 8. 不足与局限：实验覆盖、偏差风险、应用限制

-   **实验覆盖**：仅测试了**两个领域**（代码执行和机器人）。对于更复杂的场景，如Web代理、数据库代理等，其有效性有待验证。
-   **数据偏差风险**：虽然在两个领域上有效，但数据集规模较小。代码域的不安全轨迹来自特定基准（RedCode），安全轨迹是生成的，可能无法完全代表真实世界中所有安全/不安全行为的多样性。
-   **未明确保证全局最优**：论文指出算法不保证全局最优解，解决方案的质量依赖于谓词库的丰富程度。如果谓词库本身缺乏关键的区分性谓词，方法的性能上限将受到限制。
-   **可扩展性与计算成本**：ILP求解器ILASP有一定的计算开销（论文中设置了120秒超时）。在处理更大规模数据集或更复杂的逻辑假设时，ILP的求解时间可能成为瓶颈。
-   **局限性**：
    -   **处理时序和状态依赖的局限**：当前方法主要评估单个事件及其局部上下文的谓词。对于需要理解事件序列或全局状态的复杂安全场景（如数据窃取），可能需要扩展支持时序逻辑。
    -   **对ILP求解器的依赖**：性能很大程度上取决于ILP求解器的能力和效率。不同ILP求解器的性能差异可能影响AutoSpec的最终效果。
    -   **谓词库的构建和维护**：虽然论文说集成新工具只需添加谓词，但构建一个高质量、能覆盖所有潜在风险的谓词库本身仍需要一定的专家知识和持续维护工作。

（完）
