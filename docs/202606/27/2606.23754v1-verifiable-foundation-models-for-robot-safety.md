---
title: Verifiable Foundation Models for Robot Safety
title_zh: 可验证的机器人安全基础模型
authors: "Davide Corsi, Kyungmin Kim, Roy Fox"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23754v1"
tags: ["query:agent-safety"]
score: 9.0
evidence: 基于基础模型的可验证机器人安全框架
tldr: 部署基础模型控制机器人时，其强大表达能力导致模型不透明、难以形式验证。FEARL框架通过模块化分解，将策略分为大尺寸控制器和小型安全模块，安全模块接收低维安全传感器观测和控制器上下文嵌入，可被现有工具形式验证，而控制器保留任务推理能力。在三个模拟域和物理机器人上验证了有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23754v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 716, \"height\": 435, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23754v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 592, \"height\": 281, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23754v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 518, \"height\": 517, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23754v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 860, \"height\": 227, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23754v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 766, \"height\": 274, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23754v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 654, \"height\": 233, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23754v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 874, \"height\": 452, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23754v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1172, \"height\": 277, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23754v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1348, \"height\": 238, \"label\": \"Table\"}]"
motivation: 基础模型用于机器人控制时，表达能力强但难以形式验证，现有验证工具无法处理。
method: 将策略分解为大型控制器(C)和小型安全模块(S)，S接收低维安全传感器观测和C的上下文嵌入，输出最终动作。
result: 在三个模拟域中验证了FEARL的有效性，并成功将策略迁移到物理机器人。
conclusion: FEARL使得形式验证可应用于安全模块，同时保留控制器的表达能力和任务多样性。
---

## 摘要
将基础模型用于机器人控制引发了一个核心挑战：表达能力强、能够实现丰富多模态感知的模型同时使得这些模型不透明且难以形式化分析，导致现有验证工具无法处理。在本文中，我们提出了FEARL（Foundation-Enabled Assured Robot Learning，基础赋能确信机器人学习）框架，通过模块化架构分解来解决这一矛盾。FEARL将策略分为两部分：一个大型控制器（C），负责高维感知和任务推理；一个小型安全模块（S），接收来自专用安全传感器的低维观测以及来自C的有界上下文嵌入，并产生最终动作。由于许多机器人安全需求（如避障和工作空间边界约束）可以基于这些安全传感器观测来表达，因此形式验证可以应用于S而非整个基础模型主干。这使得现有工具能够进行形式分析，同时保留控制器在任务推理中的表达能力。为了证明分解后的策略仍然能够解决多种任务，我们在三个模拟机器人领域中使用多种控制器主干和训练流程（包括预训练的现成视觉-语言-动作模型）评估了FEARL。我们进一步将学到的策略从其中一个模拟任务迁移到物理机器人，表明低维安全接口支持实际的仿真到现实迁移。

## Abstract
Deploying foundation models for robot control raises a central challenge: the expressive power that enables rich, multimodal perception also makes these models opaque and difficult to analyze formally, rendering them intractable for existing verification tools. In this paper, we present FEARL (Foundation-Enabled Assured Robot Learning), a framework that addresses this tension through a modular architectural decomposition. FEARL separates the policy into a large Controller (C) responsible for high-dimensional perception and task reasoning, and a small Safety module (S) that receives low-dimensional observations from dedicated safety sensors together with a bounded context embedding from C and produces the final action. Since many robot safety requirements, such as collision avoidance and workspace boundary constraints, can be expressed over these safety sensor observations, formal verification can be applied to S rather than to the full foundation-model backbone. This makes formal analysis tractable with existing tools while preserving the Controller's expressive power for task reasoning. To show that the decomposed policy remains capable of solving diverse tasks, we evaluate FEARL on three simulated robotic domains using multiple Controller backbones and training procedures, including pretrained off-the-shelf vision-language-action models. We further transfer the learned policy from one of our simulated tasks to a physical robot, suggesting that the low-dimensional safety interface supports practical sim-to-real transfer.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：部署基础模型（如LLM、ViT、VLA）控制机器人时，其强大的表达能力带来了丰富的多模态感知，但同时也使模型成为黑箱，难以用现有形式化验证工具分析（如Marabou、α,β-CROWN）。传统方法（如安全奖励、约束强化学习、对抗性数据增强）只能提供经验性安全，无法给出形式化保证；运行时屏蔽（shield）虽然能提供更强保障，但会引入计算代价、策略干扰，且无法预先量化哪些区域已经安全。
- **整体含义**：需要一种架构，使得基础模型驱动的机器人既能保留高维感知和任务推理能力，又能被形式验证工具处理，从而实现可信的安全保证。

## 2. 方法论：核心思想、关键技术细节

### 核心思想：模块化分解（C/S分解）
- 将策略分为两个模块：
  - **Controller（C）**：处理高维输入（图像、语言指令等），输出一个低维、有界的上下文嵌入 \( z_t \in [-1,1]^{d_c} \)。
  - **Safety module（S）**：接收低维安全传感器观测 \( s_t \)（如LiDAR距离、机器人位姿）和上下文嵌入 \( z_t \)，输出最终动作。S保持小型化（如2层MLP，每层32个神经元），便于形式验证。
- 关键洞察：许多安全约束（避障、边界约束）可直接基于低维安全传感器信号表达，无需依赖高维感知。

### 关键技术细节
- **验证引导的运行时屏蔽（Verification-Guided Shielding）**：
  - **离线阶段**：使用扩展的ϵ‑ProVe工具对S进行形式验证，将联合输入域 \( \mathcal{X} = \mathcal{S} \times \mathcal{Z} \) 划分为：
    - 可证明安全区域 \( \mathcal{X}_{\text{certified}} \)：S在所有可能的上下文嵌入下都安全。
    - 未认证区域 \( \mathcal{X}_{\text{uncertified}} \)：无法保证安全。
  - **在线阶段**：若当前状态 \( (s_t, z_t) \in \mathcal{X}_{\text{certified}} \)，直接执行S输出的动作；否则启用屏蔽，当S输出不安全动作时替换为安全替代动作。
- **理论保证**（三个命题）：
  - **概率安全保证**：屏蔽策略的违规率上限为 \( \rho \bar{d}_{\bar{\pi}}^T \mu(\mathcal{X}_{\text{certified}}) \)。
  - **屏蔽激活界**：期望屏蔽激活率 ≤ \( \bar{d}_{\pi}^T \mu(\mathcal{X}_{\text{uncertified}}) \)。
  - **价值退化界**：屏蔽策略的累计回报相比未屏蔽的下降不超过 \( T \bar{d}_{\pi}^T \mu(\mathcal{X}_{\text{uncertified}}) R_{\max} \)。
  - 这些界表明：未认证区域的体积（\( \mu(\mathcal{X}_{\text{uncertified}}) \)）越小，屏蔽的干扰和性能损失越小。

## 3. 实验设计

### 场景与平台
- **Playground**：2D合成任务，智能体在1048×1048像素边界内移动至语言指定的目标区域。安全传感器：归一化位置 \( s_t \in [0,1]^2 \)。
- **Indoor Navigation（定制控制器）**：Hello Robot Stretch 2在6m×6m室内环境导航，避开障碍物。安全传感器：11束LiDAR扫描（180°前向视场），\( s_t \in [0,3]^{11} \)。
- **Indoor Navigation（VLA模型）**：使用SmolVLA作为Controller，其他设置同上。
- **Outdoor Navigation**：Unitree GO2四足机器人在16m×16m室外环境到达目标。安全传感器：归一化位姿 \( s_t = [s_x, s_y, s_\psi] \in [0,1]^3 \)。
- **真实机器人迁移**：在物理Stretch 2上部署Indoor Navigation策略（无地图版本），18个episode。

### 控制器骨干与训练方法
| 环境 | 控制器骨干 | 训练方法 | 上下文维度 |
|------|------------|----------|------------|
| Playground | BERT + ViT | PPO + LoRA | 64 |
| Indoor (Custom) | LLM + ViT | SFT → PPO | 12（结构化） |
| Indoor (VLA) | SmolVLA | PPO + LoRA | 16（学习得到） |
| Outdoor | ViT | DAgger（教师蒸馏） | 128 |

### 对比方法
- 主要对比：**有无屏蔽**（Unshielded vs Shielded）。报告成功率（SR）、违规率（VR）、屏蔽覆盖率和屏蔽激活率。
- 未与其他端到端安全方法（如约束RL、传统屏蔽）直接对比，侧重于验证自身方法的可行性和性能保持。

### 安全规格
- Playground：4个边界约束（距离边界10%内禁止向外方向）。
- Indoor：5个LiDAR条件约束（如左前方障碍物禁止左转和前进）。
- Outdoor：10个边界-方位约束（如靠近边界且面对外时禁止前进）。

## 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、数量、训练时长。仅提到了ϵ‑ProVe配置（点云尺寸3500，深度上限20，目标假阳性率0.5%，置信度99.99%），以及离线验证耗时（22.7秒到3271.2秒不等）。未提及训练阶段算力需求。

## 5. 实验数量与充分性
- **模拟实验**：三种环境各进行100个episode测试，取三个随机种子平均。
- **消融实验**：比较有无屏蔽的性能，以及不同控制器变体（Custom vs VLA）。
- **离线验证**：报告了认证体积百分比和计算时间。
- **真实机器人实验**：18个episode，有屏蔽和无屏蔽对比。
- **充分性评估**：
  - **优点**：覆盖了不同复杂度（2D简单到6D室内）、不同传感器（位置、LiDAR、位姿）、不同控制器（自定义、VLA、蒸馏）和训练范式（PPO、SFT、DAgger），展示了框架兼容性。
  - **不足**：未与基线方法（如直接端到端训练的安全RL、其他验证引导屏蔽）进行定量比较，缺少消融控制器大小或安全传感器维度的实验，未分析上下文嵌入维度对验证效率的影响。真实机器人实验样本量较小（18 episodes），可能不够充分。

## 6. 主要结论与发现
- FEARL的C/S分解使得基础模型策略**可被形式验证**：小型Safety模块S的认证体积较大（78.9%~99.4%），验证时间虽长但完全在离线完成。
- 验证引导屏蔽实现了**零运行时违规**，同时保持低屏蔽激活率（0.09%~1.13%），策略性能损失很小（成功率下降0~11.9个百分点）。
- **零样本sim-to-real迁移成功**：物理机器人上安全机制有效，碰撞率从无屏蔽的27.8%降至0%，成功率保持61.1%。
- 理论界（价值退化、激活率）与实验观察一致，未认证区域小则干扰小。

## 7. 优点
- **创新性架构**：巧妙分离感知/推理与安全决策，使形式验证从不可能变为可行，同时保留基础模型的表达能力。
- **理论保证扎实**：给出三个关于安全、激活率和性能下界的概率命题，与实验相互支撑。
- **验证引导屏蔽**：相比传统运行时屏蔽，离线认证缩小了需要干预的区域，降低了在线开销和策略干扰。
- **高度模块化与兼容性**：支持多种控制器骨干（BERT+ViT、LLM+ViT、SmolVLA）和训练方式（PPO、SFT、DAgger），方便集成不同基础模型。
- **真实世界验证**：展示了从仿真到硬件的零样本迁移，证明低维安全接口的实用性和鲁棒性。

## 8. 不足与局限
- **安全约束范围有限**：仅处理可被低维安全传感器直接观测的约束（如距离、位姿），无法处理语义级安全（如“易碎品不可放桌边”）或更复杂的时空/多智能体规范。
- **未与现有安全方法充分比较**：缺少与约束RL、传统屏蔽、其他验证方法（如直接验证端到端小网）的定量对比，难以评估FEARL的相对优势。
- **实验规模有限**：真实机器人测试样本较少（18 episodes），模拟环境均为规则几何场景，未在更复杂、非结构化环境（如家庭、工厂）中验证。
- **计算代价**：离线验证时间随安全传感器维度增加而显著增长（11维LiDAR需27~45分钟），对于更高维或连续动作空间可能不现实。
- **上下文嵌入的人工定义问题**：对于定制控制器，上下文向量是人工设计的结构化特征（如目标位置、目标one-hot），限制了端到端学习潜力；VLA变体则学习16维嵌入，但通用性待验证。
- **sim-to-real性能下降**：成功率从模拟的73%~80%降至61.1%，归因于定位漂移和传感器噪声，但论文未提供缓解措施。

（完）
