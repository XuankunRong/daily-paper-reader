---
title: Autoformalization of Agent Instructions into Policy-as-Code
title_zh: 智能体指令自动形式化为策略即代码
authors: "Adam Mondl, Matthew Maisel, John H. Brock"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26649v1"
tags: ["query:agent-safety"]
score: 9.0
evidence: 将代理指令自动形式化为正式验证的策略代码，直接确保代理安全
tldr: 智能体在高风险场景中需要正式策略保障安全，但现有概率性护栏缺乏保证，手工编码策略难以扩展。提出自动形式化流水线，通过LLM生成器-评论器循环将智能体指令、工具描述和自然语言策略转换为Cedar策略语言的形式化验证策略。在MedAgentBench基准上，自动形式化策略比手工编码策略覆盖更全面的自然语言规范。该工作为智能体安全提供可扩展、可验证的策略形式化方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26649v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1781, \"height\": 322, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26649v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1801, \"height\": 2366, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26649v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 864, \"height\": 370, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26649v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 866, \"height\": 277, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26649v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1429, \"height\": 1173, \"label\": \"Table\"}]"
motivation: 现有智能体安全方法依赖无保证的概率性护栏或不可扩展的手工编码策略，亟需自动化的正式策略生成。
method: 使用LLM生成器-评论器循环，将智能体提示、MCP工具描述和自然语言策略文档自动转换为Cedar策略语言的正式验证策略。
result: 在MedAgentBench基准上，自动形式化策略比手工编码的符号执行策略覆盖更多源自然语言规范。
conclusion: 该自动形式化流水线有效提升了策略覆盖率与可扩展性，为智能体安全提供了可靠的形式化保障。
---

## 摘要
在高风险领域中，智能体安全需要正式的策略执行，但现有方法要么依赖概率性护栏（微调分类器、基于提示的引导）而无法提供形式化保证，要么依赖手工编码的符号执行，难以扩展到真实策略规范的广度。我们提出了一种自动形式化流水线，利用基于LLM的生成器-评判器循环，将智能体提示、MCP工具描述和自然语言策略文档转化为经过形式化验证的策略。生成的策略采用Cedar策略语言编写。在MedAgentBench基准上，我们的自动形式化策略比先前工作中手工编码的符号执行覆盖了更多的源自然语言规范内容。

## Abstract
Agent safety in high-stakes domains requires formal policy enforcement, but most existing approaches either rely on probabilistic guardrails (fine-tuned classifiers, prompt-based steering) that offer no formal guarantees, or on hand-coded symbolic enforcement that does not scale to the breadth of real policy specifications. We present an autoformalization pipeline that translates agent prompts, MCP tool descriptions, and natural language policy documents into formally verified policies using an LLM-based generator-critic loop. The resulting policies are written in the Cedar Policy Language. On the MedAgentBench benchmark, our autoformalized policies cover substantially more of the source natural-language specification than the hand-coded symbolic enforcement in prior work.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLM）驱动的智能体（agent）在自主执行多步任务时，常需获得高权限，从而面临严重的安全风险（如提示注入）。现有安全护栏方法（如微调分类器、提示引导）虽能一定程度缓解风险，但均为概率性，无法提供形式化保证；而手工编写的符号执行规则虽能提供确定性，却难以覆盖真实策略的广度与复杂业务逻辑，缺乏可扩展性。
- **整体含义**：本文旨在通过“自动形式化（Autoformalization）”技术，将自然语言表达的智能体指令、工具描述及策略文档自动转化为形式化验证的策略代码（Policy-as-Code），实现**可扩展、可验证、确定性**的智能体行为监控，从而在高风险领域（如医疗）提供可靠安全保障。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：采用“Verification Sandwich”分层架构，将自动形式化过程分解为基础层、模型层和安全层，并通过生成器-评判器循环迭代优化，最终输出由Cedar策略语言编写的正式策略。
- **关键技术细节**：
  - **基础层（Grounding Layer）**：从智能体卡片、MCP工具描述中抽取实体、工具模式，定义主体-资源-动作本体，并程序化生成Cedar模式（schema），确保所有潜在动作映射到真实实体和系统标识。
  - **模型层（Model Layer）**：利用LLM（文中为Gemini 3 Pro）生成候选Cedar策略集合，输入包括系统提示、工具定义、自然语言策略文档以及自动生成的Cedar schema。
  - **安全层（Safety Layer）**：包含两种评判器：
    - **硬评判器（Hard Critic）**：利用Cedar官方的语法检查、schema合规检查、静态分析（如检测空策略、矛盾规则），进行确定性验证。
    - **软评判器（Soft Critic）**：通过LLM-as-a-Judge（文中为Gemini 2.5 Flash）评估候选策略与原自然语言意图的语义对齐质量，按预定评分标准给出反馈。
  - **生成器-评判器循环**：硬/软评判器的反馈被回传给策略生成器，迭代改进，直至通过所有硬评判条件（语法、schema、无矛盾）且达到软评判的质量阈值，最终输出经过验证的Cedar策略集合。
  - **运行时执行**：使用外部确定性策略引擎（基于Cedar）拦截并deny违反策略的工具调用，实现fail-closed机制——发生错误时默认阻止操作。

## 3. 实验设计：数据集、基准、对比方法

- **基准**：MedAgentBench（电子病历EMR智能体能力基准），原始版本不包含安全策略。由Hong等人（2026）为其编写了一份包含88条规则的自然语言合成策略（由GPT-5.2根据危险分析生成），并手工实现了其中23条规则的符号护栏。
- **数据集与条件**：
  - **原始（Original）数据集**：300条良性任务轨迹。
  - **对抗（Adversarial）数据集**：50条包含恶意提示以诱导违规的轨迹。
  - 三种执行条件：
    - **Raw**：智能体直接使用原始HTTP GET/POST工具（MedAgentBench默认）。
    - **Baseline**：每个HTTP端点包装为类型化的MCP工具。
    - **Guardrail**：在Baseline基础上添加Hong等人手工编写的符号护栏。
- **对比方法**：主要对比Hong等人手工编写的符号护栏（覆盖23条规则），与本文自动形式化流水线生成的Cedar策略（通过同一自然语言策略文档自动生成，额外从MCP服务器中提取了一些FHIR约束）。
- **评估指标**：
  - Hong等人的“Unsafe Rate”：违反其23条手工规则的轨迹比例。
  - 本文的“Cedar Block Rate”：被自动形式化Cedar策略阻止的轨迹比例。

## 4. 资源与算力

- 文中**未明确说明**使用的GPU型号、数量、训练时长等算力信息。
- 仅提及候选生成器为**Gemini 3 Pro**，软评判器采用**Gemini 2.5 Flash**（温度分别为0.3和0.1）。硬评判器调用Cedar Rust CLI工具。
- 未报告生成每条策略的计算成本或推理时间。

## 5. 实验数量与充分性

- **实验组数量**：在原始（300条）和对抗（50条）两个数据集上，分别测试了三种条件（Raw/Baseline/Guardrail），共6组主要对比（Table 1）。此外，还按轨迹是否包含写请求（POST）进行了二次分解（Table 2），以及按规则来源（policy.md vs MCP）统计触发次数（Table 3）。
- **充分性评价**：
  - 实验设计较为清晰，对比了自动形式化与手工编码的覆盖能力差异，并通过对抗数据集验证了鲁棒性。
  - **局限性**：
    - 仅与Hong等人的手工编码方法对比，缺少与其他自动形式化方法（如直接生成Python校验、其他策略语言）的基线。
    - 未进行消融实验（如只使用硬评判器 vs 同时使用软评判器、不同LLM生成器对比等）。
    - 仅在单一基准（MedAgentBench）上评估，领域特异性强，泛化性未知。
    - 未评估智能体**效用下降**（即过于严格的策略是否导致过多合法请求被拒绝），作者虽然讨论了“摩擦-效用权衡”，但未定量分析。

## 6. 论文的主要结论与发现

- **主要结论**：自动形式化流水线能够将自然语言策略自动转化为形式化Cedar策略，且覆盖范围显著超过手工编码的符号护栏。
- **关键数据**：
  - 在Original数据集的Guardrail条件下，Hong等人的Unsafe Rate为0%（由构造保证），而Cedar Block Rate为55.7%，表明自动形式化覆盖了更多原策略规则（约88条中的大部分而非仅23条）。
  - 在Adversarial Guardrail条件下，Cedar Block Rate达到85.7%（42/49），Hong等人仍为0%。
  - 当分解到**包含写请求（POST）的轨迹**时，Cedar Block Rate在大部分条件下接近100%（如Original Raw: 100%, Baseline: 94%, Guardrail: 98.8%；Adversarial所有条件100%），说明未阻止的轨迹均为无需写操作的检索任务。
- **额外发现**：将策略执行从LLM上下文窗口解耦至确定性策略引擎，可有效抵御提示注入和越狱攻击；fail-closed机制进一步保证安全优先。

## 7. 优点：方法或实验设计上的亮点

- **方法创新**：
  - 提出“Verification Sandwich”分层自动形式化架构，将基础建模、LLM生成、形式化验证三者有机结合，既有LLM的灵活性又保障了输出的正确性。
  - 硬/软评判器双机制：硬评判确保语法和逻辑正确性，软评判评估语义对齐，形成闭环自改进。
  - 选择Cedar语言：支持形式化验证、静态分析、人类可读，且已被AWS开源，具有较强的实用性。
- **实验亮点**：
  - 使用对抗数据集测试鲁棒性，展示自动形式化策略在面对恶意提示时的更高覆盖率。
  - 通过按写请求分解分析，清晰区分了可阻止与无法阻止的轨迹（纯检索任务），避免了过度解读。
  - 开源了策略执行工具和Cedar Python绑定，便于复现和扩展。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅在一个医疗领域基准上评估，未在多个不同领域或更复杂策略上验证泛化性。
  - 缺乏与自动化基线（如直接使用LLM生成JSON校验规则、RBAC规则等）的对比，难以评估该方法相对其他轻量级方案的精确优势。
  - 未评估智能体效用损失：高覆盖率可能伴随着过阻塞，作者虽讨论了“摩擦-效用权衡”，但未提供实际效用指标（如任务成功率）。
- **依赖LLM的不确定性**：
  - 生成器和软评判器均依赖商业LLM（Gemini），输出可能不稳定，且软评判的评分标准依赖人工设定，可能引入偏差。
  - 生成器-评判器循环次数未说明，迭代可能产生额外成本且无法保证终止。
- **Cedar语言本身的限制**：
  - Cedar是**无状态**的，不天然支持多轮依赖、时序逻辑（如“必须先执行A再执行B”），作者虽提出未来工作通过实体存储和记忆感知政策解决，但当前版本仅能基于单步上下文和预计算特征（通过SessionContext嵌入状态）。
- **手工编码基线可能不公**：Hong等人只手工实现了23条规则，而自动形式化策略覆盖了大部分88条规则，直接比较Block Rate与Unsafe Rate存在覆盖范围的不对称。虽然作者声明这是为了展示覆盖优势，但未在同一规则子集上做公平对比。
- **计算成本未报告**：缺乏LLM生成和评判的延迟、费用等经济成本信息，影响实际部署可行性。

（完）
