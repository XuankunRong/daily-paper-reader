---
title: "SAFARI: Scaling Long Horizon Agentic Fault Attribution via Active Investigation"
title_zh: SAFARI：通过主动调查扩展长时域智能体故障归因
authors: "Chenyang Zhu, Jiayu Yao, Kushal Chawla, Youbing Yin, Nathan Wolfe, Pengshan Cai, Jingyu Wu, Spencer Hong, Sangwoo Cho, Shi-Xiong Zhang, Daben Liu, Sambit Sahu, Erin Babinsky"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24626v1"
tags: ["query:agent-safety"]
score: 9.0
evidence: 自主代理故障归因，直接支持代理安全评估
tldr: "自主代理在执行长时多步任务时轨迹超长，现有诊断方法依赖全轨迹加载，受限于上下文窗口且注意力稀释。SAFARI提出工具增强的诊断循环，通过搜索读取片段和短期记忆主动归因故障。在Who&When数据集上1M token预算下提升20%，TRAIL GAIA子集25K预算下提升19%，在超长轨迹（5倍上下文）中仍保持0.58精度。该方法有效解耦诊断与上下文限制，显著提升可扩展性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24626v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1654, \"height\": 572, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24626v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 861, \"height\": 461, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24626v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 857, \"height\": 462, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24626v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 873, \"height\": 272, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24626v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 870, \"height\": 312, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24626v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1536, \"height\": 784, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24626v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1741, \"height\": 650, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24626v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1020, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24626v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1286, \"height\": 409, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24626v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1798, \"height\": 64, \"label\": \"Table\"}]"
motivation: 代理轨迹超出现有上下文窗口，全轨迹加载导致注意力稀释和诊断失败。
method: 用工具箱（读取/搜索轨迹片段）和短期记忆形成主动诊断循环，替代线性上下文加载。
result: "Who&When提升20%，TRAIL GAIA提升19%，超5倍上下文精度0.58，传统方法完全失败。"
conclusion: SAFARI解耦诊断精度与上下文限制，实现可扩展的长时任务故障归因。
---

## 摘要
随着自主智能体处理日益复杂的多步骤、多智能体任务，其执行轨迹的规模已经超出了最大上下文窗口的限制。当前有效诊断智能体故障的方法是将完整轨迹加载到LLM的上下文窗口中，但这种方法存在注意力稀释问题，并且在智能体轨迹不可避免地超出上下文限制时失效。为了解决这一问题，我们提出了SAFARI（通过主动调查扩展长时域智能体故障归因），该框架用工具增强的诊断循环替代了线性上下文加载。通过为LLM配备专门的工具箱来读取和搜索轨迹片段，同时配备持久短期记忆（STM）用于跨轮推理，SAFARI有效地将诊断准确性与架构上下文限制解耦。我们的实验表明，在1M token预算内，SAFARI在Who&When数据集上以20%的优势超越了现有最佳结果；在25K token预算内，在TRAIL GAIA子集上以19%的优势领先。最显著的是，即使目标故障位于模型原生上下文窗口5倍之外，SAFARI仍能保持0.58的精确度，而传统评估方法在此情况下完全失效。

## Abstract
As autonomous agents tackle increasingly complex multi-step, multi-agent tasks, their execution trajectories have scaled beyond the constraints of even the largest context windows. Current methods for effectively diagnosing agent failures load the full trajectory into an LLM's context window, which suffers from attention dilution and fails when agentic traces inevitably exceed context limits. To address this, we introduce SAFARI (Scaling long-horizon Agentic Fault AttRibution via active Investigation), a framework that replaces linear context loading with a tool-augmented diagnostic loop. By equipping LLMs with a specialized toolbox to read and search trajectory segments alongside a persistent Short-Term Memory (STM) for cross-turn reasoning, SAFARI effectively decouples diagnostic accuracy from architectural context limits. Our experiments demonstrate that SAFARI outperforms state-of-the-art results by 20% on the Who&When dataset within a 1M token budget, and by 19% on TRAIL GAIA subset on a 25K token budget. Most significantly, SAFARI maintains a 0.58 precision even when the target fault resides 5x beyond the model's native context window, a scenario where traditional evaluators fail entirely.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：当前自主智能体（LLM-driven agents）在执行复杂多步骤、多智能体任务时，轨迹长度急剧增长，甚至超过最大上下文窗口（如百万token级别）。传统的故障归因方法将整个轨迹加载到LLM上下文中，导致注意力稀释并在轨迹超限时彻底失效。亟需一种能够**不受上下文窗口限制、可扩展的故障归因框架**。
- **整体含义**：SAFARI通过**主动调查**（active investigation）机制，将故障诊断从被动全轨迹阅读转变为交互式、假设驱动的搜索，使得诊断准确性与架构上下文限制解耦，从而在轨迹远超模型原生上下文时依然保持高性能。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：用一个**调查者智能体**（Investigator Agent）替代传统的全轨迹加载，它配备专门的工具箱（read、search）和**短期记忆**（Short-Term Memory, STM），通过迭代提出假设、搜索证据、验证假设的循环来定位故障。
- **关键技术细节**：
  1. **工具箱**：
     - `read(offset, limit)`：模拟人“滚动”日志，顺序读取轨迹片段。
     - `search(pattern)`：基于正则表达式的搜索，允许智能体瞬间跳转到特定span ID、变量值或错误签名，避免消耗上下文窗口。
  2. **短期记忆（STM）**：在每次LLM调用时追加到对话历史末尾，包含四个字段：任务目标、故障假设与证据、调查缺口、过去工具调用。当上下文预算耗尽时，最旧的轮次会被静默丢弃，但STM能持久化关键信息，保证跨轮推理的连贯性。
  3. **验证阶段**：调查者将假设分解为最多3个原子声明，并行发送给**推理评估器**（Reasoning Evaluator LLM）。评估器仅根据引用的证据判断声明是否成立，输出置信度分数。平均置信度≥θ（默认70）时认为证据充分，可提交最终答案；否则低分声明指导下一步调查。
  4. **迭代终止条件**：达到有效结论并提交，或达到迭代预算K=30次后强制生成最佳答案。

- **算法流程（文字说明）**：
  - 初始化历史H₀（系统提示+用户提示，包含轨迹总长度、元数据等）。
  - 每轮k：调查者观察当前历史Hₖ和工具模式T，产生一个工具调用a（read或search）。
  - 工具返回观察o，追加到历史形成Hₖ₊₁。
  - 如果证据充分，调查者提出原子声明，调用evaluate()让评估器验证。
  - 若平均置信度≥θ，调用submit_step()输出最终故障位置t'和理由R'；否则继续迭代。
  - 达到迭代预算则强制合成最佳答案。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：
  - **Who&When**：包含算法生成子集（Magentic-One，126条轨迹，平均3.3k tokens）和人工编排子集（CaptainAgent，58条轨迹，平均19k tokens，最长130步）。
  - **TRAIL**：长时域智能体轨迹，包含GAIA子集（116条，平均228k tokens，最长2.2M tokens、122步）和SWE-bench子集（31条，平均619k tokens，最长2.0M tokens、29步）。
- **基准（Baselines）**：
  - **One-shot Prompting**：将整个轨迹加载到LLM上下文中的标准CoT提示。
  - **Step-by-step**：逐步扫描每一步直至定位故障，可绕过全局上下文限制。
  - **RAFFLES**：当前最先进的迭代式归因方法，但仍受限于模型原生上下文窗口。
- **评估指标**：
  - Who&When：步骤级准确率（Step-level Accuracy）和容忍准确率（Tolerant Accuracy ±s steps）。
  - TRAIL：Precision（预测步骤是否与任何真实故障对齐）和Strict Precision（是否与高影响故障对齐）。
- **评估设置**：
  - **固定上下文预算**：在25k、100k、200k、1M tokens下评估。
  - **固定缩放因子ς**：定义ς = 故障token位置 / 模型上下文长度。在ς=1.25x, 1.6x, 2.5x, 5x下评估，使得故障位置超出模型上下文窗口。

## 4. 资源与算力
- 论文中未明确说明使用的GPU型号、数量及训练时长。仅提及：
  - 骨干LLM为**Claude-Opus-4.6**（1M token上下文窗口），通过API调用。
  - 推理阶段产生工具调用成本（latency分析见表5：GAIA 25K预算下SAFARI耗时267.21秒，1M预算下31.43秒）。
- 未提及任何模型训练或微调，全部为基于预训练LLM的推理方法。因此算力消耗主要体现在LLM API调用和工具执行上。

## 5. 实验数量与充分性
- **实验组数**：
  1. **固定预算实验**：在TRAIL GAIA和SWE-bench两个子集上，各4种预算（25k/100k/200k/1M），对比4种方法（one-shot, step-by-step, RAFFLES, SAFARI），共32组主要结果（表3）。
  2. **Who&When实验**：同样4种预算×2个子集×4种方法共32组（表4），并报告±5步容忍准确率。
  3. **固定缩放因子实验**：在TRAIL GAIA和SWE-bench上，4种缩放因子（1.25x, 1.6x, 2.5x, 5x），仅报告SAFARI（表2）。
  4. **消融实验**：在TRAIL GAIA和SWE-bench上，对比完整SAFARI vs 无STM版本（表6），覆盖3种预算。
  5. **延迟分析**：表5展示4种方法×3种预算的延迟。
- **充分性与公平性**：
  - 实验覆盖了从短轨迹（Who&When Algo 3.3k tokens）到极长轨迹（TRAIL GAIA 2.2M tokens）的多种规模，设置全面。
  - 对比了最先进方法（RAFFLES）和传统方法（one-shot, step-by-step），并采用相同骨干LLM。
  - 消融实验验证了STM组件的有效性。
  - 固定缩放因子实验模拟了极端长尾情景，弥补了现有数据集中故障过早的不足。
  - 但SWE-bench子集样本量较小（31条），可能导致统计波动；且所有实验仅涉及单一LLM（Claude-Opus-4.6），未测试其他模型泛化性。

## 6. 论文的主要结论与发现
- **主要结论**：SAFARI通过主动调查循环，在资源受限环境下显著优于现有方法，并能处理超长轨迹（5倍上下文窗口外仍保持0.58 precision）。
- **具体发现**：
  - 在Who&When Hand-crafted子集上（1M token预算）比RAFFLES提升20%（±1步准确率0.414 vs 0.345）。
  - 在TRAIL GAIA（25k token预算）上Strict Precision比RAFFLES高19%（0.5043 vs 0.4188）。
  - 当轨迹完全在上下文中时，SAFARI略逊于RAFFLES（1M预算下Strict Precision 0.5128 vs 0.5641），源于STM模块的信息损失。
  - 在代码密集的SWE-bench子集上，STM效果减弱，尤其是在高预算下；但在低预算时优势显著。
  - 固定缩放因子实验证明SAFARI在ς≥1时仍能正常工作，而传统方法彻底失效。

## 7. 优点：方法或实验设计上的亮点
- **方法亮点**：
  - **主动调查范式**：将故障归因从被动阅读转变为交互式搜索，从根本上突破上下文限制。
  - **短期记忆（STM）**：持久化关键信息，支持跨轮推理，避免早期证据因上下文丢弃而丢失。
  - **原子推理评估器**：将假设分解为可验证的子声明，并行评估，提供细粒度反馈，指导调查方向。
  - **工具设计**：read和search工具模拟人类调试行为，高效导航大规模轨迹。
- **实验设计亮点**：
  - 引入**缩放因子ς**和**固定上下文预算**两种评估协议，系统衡量系统在极端长尾场景下的鲁棒性。
  - 覆盖两个互补基准（Who&When用于中等长度、TRAIL用于超长长度），评估全面。
  - 包含消融实验和延迟分析，提供多维度理解。

## 8. 不足与局限
- **实验覆盖不足**：
  - 仅使用单一LLM（Claude-Opus-4.6），未测试其他模型（如GPT-4、开源模型）的泛化性。
  - SWE-bench子集样本量小（31条），可能导致结果波动。
  - 未在更大规模、更多样化的真实生产轨迹上评估。
  - 未考虑多故障场景或非决定性故障（如多个错误相互交织）。
- **偏差风险**：
  - 论文承认GAIA数据集的内容可能已在预训练语料中（如维基百科知识），导致模型“记住”正确答案；虽然不影响故障定位（模型不知道哪一步出错），但可能高估了对最终答案的推理能力。呼吁使用私有数据集进一步验证。
  - STM在高预算场景下存在信息损失与原始日志的权衡，可能导致性能下降。
- **应用限制**：
  - 延迟较高（25k预算下GAIA需267秒），不适合实时监控。
  - 对代码密集轨迹的总结能力较弱，需要更好的压缩策略。
  - 需要额外设计工具调用循环，增加了系统复杂度。
  - 当前仅针对离线故障归因，未讨论在线、流式环境下的应用。

（完）
