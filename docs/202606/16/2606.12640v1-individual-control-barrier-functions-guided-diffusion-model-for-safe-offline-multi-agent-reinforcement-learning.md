---
title: Individual Control Barrier Functions-Guided Diffusion Model for Safe Offline Multi-Agent Reinforcement Learning
title_zh: 个体控制屏障函数引导的扩散模型用于安全离线多智能体强化学习
authors: "Qingyun Guo, Junyi Shi, Jianuo Huang, Tianyu Shi"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.12640v1"
tags: ["query:agent-safety"]
score: 9.0
evidence: 结合控制障碍函数的离线多智能体强化学习安全方法
tldr: 离线多智能体强化学习面临安全挑战，现有扩散模型方法多关注单智能体。本文提出将个体控制障碍函数嵌入扩散模型，在轨迹生成时施加安全约束，并通过逆动力学恢复策略。在多个基准测试中，该方法在保持奖励的同时大幅提升安全性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.12640v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 901, \"height\": 514, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12640v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 689, \"height\": 374, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.12640v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1440, \"height\": 694, \"label\": \"Table\"}]"
motivation: 现有离线强化学习方法在多智能体场景下缺乏安全保障，扩散模型的应用未解决多智能体安全问题。
method: 将神经个体控制障碍函数融入扩散模型，在轨迹生成过程中施加安全约束，并通过逆动力学恢复每个智能体的控制策略。
result: 在多个多智能体基准上，该方法显著降低了不安全行为，同时奖励与基线方法相当或更优。
conclusion: 提出的算法有效实现了离线多智能体环境下的安全强化学习，平衡了安全与性能。
---

## 摘要
离线强化学习允许直接从数据中学习控制策略而无需在线交互，使其适用于安全关键任务。最近的研究将扩散模型应用于离线强化学习，以利用其对复杂数据分布的强大建模能力。然而，现有方法主要关注单智能体设置，多智能体环境中的安全挑战在很大程度上未被探索。在这项工作中，我们提出了一种安全的离线多智能体强化学习算法，该算法将神经个体控制屏障函数嵌入到扩散模型中，以在轨迹生成过程中增强安全性，并通过逆动力学恢复控制策略。我们在多个基准测试中评估了我们的算法，证明了显著的安全改进，同时保持了有竞争力的奖励。

## Abstract
Offline reinforcement learning allows control policies to be learned directly from data without online interaction, making it suitable for safety-critical tasks. Recent studies have applied diffusion models to offline reinforcement learning to leverage their strong capacity for modeling complex data distributions. However, existing approaches primarily focus on single-agent settings, leaving the safety challenges in multi-agent environments largely unexplored. In this work, we propose a safe offline multi-agent reinforcement learning algorithm that embeds neural individual control barrier functions into the diffusion model to enhance safety during trajectory generation, with control policies recovered through inverse dynamics. We evaluate our algorithm across diverse benchmarks, demonstrating substantial safety improvements while maintaining competitive rewards.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：离线多智能体强化学习（Offline MARL）中如何保证安全性，同时保持高奖励。现有离线强化学习方法主要针对单智能体，缺乏对多智能体协调与安全约束的建模。
- **背景**：安全关键应用（如机器人、自动驾驶、车辆调度）需要学习控制策略满足严格安全要求。在线安全RL需与环境交互，成本高且风险大；离线安全RL可在无交互下从数据集学习。扩散模型在离线RL中表现优越，但现有方法（如FISOR、SafeDiffuser）局限于单智能体，且依赖成本函数易引入外推误差。
- **整体含义**：提出一种将神经个体控制屏障函数（Individual CBFs）嵌入多智能体扩散模型的方法，在轨迹生成时施加安全约束，通过逆动力学恢复策略，实现安全离线多智能体RL。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：基于CTDE范式，每个智能体学习个体CBF（神经网络参数化），将其损失反向传播到扩散模型生成的轨迹中，引导轨迹朝向高奖励且安全的状态。
- **关键技术细节**：
  - **多智能体扩散模型**：基于MADIFF，对每个智能体使用特征剪枝后的全局状态与局部观测结合（记为\(\tilde{s}_i\)）作为输入轨迹\(\tau_i = [\tilde{s}_i(t), \tilde{s}_i(t+1), \dots, \tilde{s}_i(t+H-1)]\)。扩散模型以奖励r为条件，通过分类器引导生成高奖励轨迹。
  - **神经个体CBF学习**：每个智能体i学习CBF \(h_{\omega_i}(\tilde{s}_i)\)，满足三个条件：初始状态、危险状态、以及离散时间CBF条件 \(h_i(\tilde{s}_i(t+1)) \ge \alpha h_i(\tilde{s}_i(t))\)。损失函数 \(L_{C_i}(\omega_i, \theta)\) 包含三项：初始区域、危险区域、安全区域的边界保持。其中\(\alpha = m - \eta k_i\)，随时间步k递减以避免过于保守。
  - **逆动力学策略恢复**：学习逆动力学模型 \(f_{\phi_i}\) 从 \(\tilde{s}_i(t), \tilde{s}_i(t+1)\) 预测动作 \(a_i(t)\)，损失为均方误差。
- **算法流程**（Algorithm 1）：
  1. 从数据集中提取轨迹
  2. 对每个扩散步骤k从K到1：
     - 更新扩散模型参数θ（损失式6）
     - 对每个智能体i：用去噪后的\(\tilde{s}_i^\theta\)计算CBF损失，更新\(\omega_i\)和θ
     - 更新逆动力学参数\(\phi_i\)
  3. 从生成样本中计算动作。

## 3. 实验设计：使用了哪些数据集/场景，benchmark，对比方法
- **环境与数据集**：
  - **Simple Spread**（低维）：基于PettingZoo，3个智能体覆盖3个地标，避免碰撞。数据集来自Pan et al. (2021)，含Expert、Medium-Replay、Medium、Random四种质量。
  - **Safe MAMUJOCO**（高维）：包括2x3 HalfCheetah、2x4 Ant、4x2 Ant三种配置。机器人各部分由独立智能体控制，目标前进且速度不超过阈值。数据集来自Off-the-Grid（Formanek et al., 2023），含Good、Medium、Poor三种质量。
- **基准**：因缺乏安全离线MARL基线，对比了MA-TD3BC（Fujimoto & Gu, 2021）和OMAR（Pan et al., 2021）。
- **指标**：episode reward（越高越好）和episode cost（越低越好）。奖励和成本均进行了归一化。

## 4. 资源与算力
- **文中未明确说明**：论文未提及GPU型号、数量、训练时长等算力资源，仅在致谢中提及“computational resources provided by the Aalto Science-IT project”。因此无法量化算力消耗。

## 5. 实验数量与充分性
- **实验数量**：共9个任务（Simple Spread: 4个数据集；Safe MAMUJOCO: 2ant/4ant/2halfcheetah各3个质量），每个任务用5个随机种子重复，报告均值和标准差。未包含消融实验（如无CBF、不同α选择等）。
- **充分性**：
  - 覆盖低维和高维环境，数据质量多样化，具有一定代表性。
  - 但缺少与专门安全离线MARL方法的对比（因缺乏公开基线），且未在更多复杂场景（如异构、大规模智能体）验证。
  - 缺乏消融实验和分析（例如CBF损失权重、α调度的影响），降低了对方法各组件贡献的明确理解。
  - 整体实验设计客观，但充分性一般，可补充更多分析。

## 6. 论文的主要结论与发现
- 提出的算法在几乎所有任务中实现了最低的cost，同时保持competitive的reward。
- 在Simple Spread的medium-replay、medium、random数据集上几乎实现零碰撞。
- 与MA-TD3BC和OMAR相比，方法在高维环境（2ant、4ant）中奖励更高且成本更低。
- 表明神经个体CBF嵌入扩散模型能有效提升离线多智能体RL的安全性，且不显著损失性能。

## 7. 优点：方法或实验设计上的亮点
- **方法创新**：首次将个体CBF与多智能体扩散模型结合，无需学习代价函数，避免外推误差，适应不同安全约束。
- **CBF可微引导**：通过反向传播将安全损失直接注入扩散模型生成过程，实现端到端安全轨迹生成。
- **CTDE框架**：用特征剪枝的全局-局部状态兼顾协调与个体信息。
- **稀疏CBF保守度**：时间衰减的α避免局部最优，保持探索能力。
- **实验覆盖多数据集质量**：验证了方法在数据质量变化时的鲁棒性。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制
- **实验覆盖不足**：未包含消融实验（如去掉CBF、不同α设置、不同网络架构），难以量化各组件的贡献。仅在9个任务上评测，未在更复杂、异构或大规模多智能体场景测试。
- **基线对比局限**：缺乏专门的安全离线MARL基线（仅对比普通离线MARL方法），且MA-TD3BC在Simple Spread-random中可能因未完成任务而成本低，结论需谨慎。
- **理论分析不足**：未从理论上证明训练后的扩散模型生成轨迹一定满足CBF条件，仅凭实验验证。未讨论分布外（OOD）问题及闭环安全性。
- **应用限制**：依赖逆动力学模型，需要准确状态转移；假设环境为离散时间、连续状态/动作；未处理感知不确定性和异步交互。**作者在结论中也指出**“reword and CBF guidance may still bias generated trajectories and lead to out-of-distribution issues”，并计划未来扩展到验证模块。
- **算力资源未报告**：无法评估方法可重复性及资源需求。

（完）
