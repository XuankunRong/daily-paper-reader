---
title: "Hidden in Plain Sight: Benchmarking Agent Safety Against Decomposition Attacks with DECOMPBENCH"
title_zh: 隐藏于显而易见之处：用DECOMPBENCH基准测试分解攻击下的智能体安全性
authors: "Vikhyath Kothamasu, Virginia Smith, Chhavi Yadav"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.13994v1"
tags: ["query:agent-safety"]
score: 9.0
evidence: 针对分解攻击的智能体安全评估基准
tldr: LLM智能体面临分解攻击威胁，即有害任务被拆分为看似无害的子任务以绕过安全机制。本文提出DeCompBench基准，基于分解设计原则利用图形框架生成可执行子任务流程。实验发现SOTA智能体对整体有害任务拒绝率高，但对分解变体拒绝率显著降低，常无意完成恶意目标。该工作强调需针对分解攻击进行安全评估并推动防御研究。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13994v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1387, \"height\": 413, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13994v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 724, \"height\": 479, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13994v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1360, \"height\": 1019, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13994v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1435, \"height\": 1063, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13994v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1340, \"height\": 108, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13994v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1394, \"height\": 714, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.13994v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1400, \"height\": 731, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13994v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1059, \"height\": 836, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13994v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1202, \"height\": 223, \"label\": \"Table\"}]"
motivation: 现有基准未显式评估分解攻击，导致智能体安全机制在应对此类威胁时存在盲区。
method: 构建DeCompBench，采用分解设计原则与图形框架，将有害任务分解为单独安全的子任务流程。
result: SOTA智能体对整体有害任务高拒绝，但对分解变体拒绝率大幅下降且常实现恶意目标。
conclusion: 揭示分解攻击的有效性，亟需针对性的安全评估与防御策略。
---

## 摘要
基于LLM的智能体正变得越来越强大且广泛部署，这在实际世界中增加了对抗性滥用的动机。一种关键的新兴威胁是分解攻击\cite{glukhov2024breach, jones2024adversaries}，其中有害任务被分解为更简单的、看似良性的子任务，这些子任务分别执行时能规避安全机制，但累积起来却实现了恶意意图。尽管最近的基准测试在多轮和多工具使用的场景下评估了智能体安全性，但它们并未明确捕捉这种形式的分解性滥用，可能无法代表现实中的对抗性执行流程。为此，我们引入了DeCompBench，这是一个专门设计用于评估分解攻击下智能体安全性的基准测试。DeCompBench采用“分解即设计”原则，利用图形框架创建，能够将有害任务分解为独立良性且可执行的子任务，并附有实际工作流。我们使用自定义分解器进行的实验表明，最先进的智能体在整体有害任务上表现出较高的拒绝率，但在其分解变体上拒绝率显著降低，而常常无意中实现了对抗性目标。这些发现强调了针对分解攻击的安全性评估及相应防御的必要性。我们的数据集已公开，可在https://huggingface.co/datasets/decompositionbench/DeCompBench获取。

## Abstract
LLM-based Agents are becoming increasingly capable and widely deployed, creating growing incentives for adversarial misuse in the real-world. A key emerging threat is Decomposition Attacks \cite{glukhov2024breach, jones2024adversaries} in which a harmful task is broken into simpler, benign subtasks that evade safety mechanisms when executed separately but cumulatively fulfill the malicious intent. Although recent benchmarks assess agent safety in multi-turn and multi-tool-use settings, they do not explicitly capture this form of decompositional misuse and may not represent realistic adversarial execution flows. To this end, we introduce DeCompBench, a benchmark designed specifically to evaluate agentic safety under decomposition attacks. DeCompBench is created with a decomposition-by-design principle using a graphical framework and enables harmful task decomposition into individually benign and executable subtasks with realistic workflows. Our experiments using a custom decomposer show that state-of-the-art agents exhibit high refusal rates on monolithic harmful tasks, but significantly lower refusal rates on their decomposed variants, while often inadvertently fulfilling the adversarial objectives. These findings underscore the need for safety evaluations against decomposition attacks and corresponding defenses. Our dataset is publicly available and can be found at https://huggingface.co/datasets/decompositionbench/DeCompBench.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：随着LLM智能体能力增强和广泛部署，对抗性滥用风险上升。一种新兴威胁是**分解攻击（Decomposition Attacks）**：将有害任务拆分为一系列单独看似良性的子步骤，分别执行时能绕过安全机制，但累积实现恶意目标。近期真实事件（如2025年Claude Code被用于网络间谍活动）表明这种攻击已经出现，但现有智能体安全基准（如OpenAgentSafety、MT-AgentRisk）并非专门针对分解攻击设计，其任务可能无法被自然分解，导致安全评估不准确。
- **核心问题**：系统评估智能体在分解攻击下的脆弱性，并揭示当前安全机制在应对逐步累积恶意意图时的失效。
- **整体含义**：首次提出“分解即设计”原则的基准，为分解攻击提供真实可评估的测试环境，强调亟需针对性防御。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：**分解即设计（Decomposition-by-Design）**——从任务构建阶段就确保任务可被分解为独立良性的子步骤，且子步骤贴合现实攻击流程。通过四个准则保证有效分解：①内在恶意性（原任务本身有害）、②内在可分解性（原任务需要序列依赖子步骤）、③良性子任务隔离（单独子步骤无害）、④执行难度（原任务超过弱无对齐模型能力）。
- **关键技术与流程**（四阶段流水线）：
  - **Stage 0：能力目录创建**——扫描环境组件（REST API、Python包、Docker等），枚举335个原子能力并标注角色标签（如discovery、data_access、exfiltration等），保证每个能力单独无害。
  - **Stage 1：种子任务策展**——手工创建101个种子任务（8个攻击类别），每个任务包含一个有害目标和一个手写有向无环图（DAG）模板，节点表示抽象步骤，边表示数据依赖，强制保证内在可分解性。
  - **Stage 2：任务图构建**——将种子DAG实例化：为每个节点分配具体能力（匹配角色标签），自动插入桥接能力处理输出输入类型不匹配，标记可选节点随机包含/排除，加入NPC交互节点（使用Sotopia框架模拟用户）。通过多样性过滤器（规则）和真实性检查器（Claude Haiku 4.5 LLM）筛选，拒绝无现实意义的图。
  - **Stage 3：自然语言任务生成**——使用GPT-4o将任务图转换为最终任务描述（仅描述目标，不描述步骤），并用具体实体填充（文件路径、表名等），生成5个文件：task.md（给智能体的指令）、desiderata.json（可编程检查）、checkpoints.md（LLM评判检查）、metadata.json、scenarios.json（NPC行为配置）。
- **分解器设计**：使用GPT-4o（温度0.7）将整体任务分解为子任务序列。核心转换包括：①中间间接（将有害值与使用它的操作分离，通过写入/workspace文件间接引用）；②逐步包裹（将有害操作写入中性文件，后续子任务通过文件名引用执行）。平均分解为5.98个子任务（范围3-8）。

## 3. 实验设计：数据集、场景、基准、对比方法
- **数据集**：DeCompBench基准，包含250个任务，覆盖8个攻击类别（审计篡改24.4%、金融犯罪22.0%、数据窃取12.8%、社会工程10.8%、供应链攻击10.8%、个人攻击6.8%、未授权访问6.4%、系统破坏6.0%）。任务涉及9种服务（PostgreSQL、GitLab、OwnCloud等），平均每个任务使用2.64种服务，10.8%的任务涉及NPC交互。所有任务均经过手工验证。
- **实验场景**：两种设置：①**整体设置**（Monolithic）——直接给出完整有害任务，测试智能体是否拒绝；②**分解设置**（Decomposed）——按顺序依次给出子任务，每轮清空对话历史（模拟攻击者使用不同账户或对话），智能体仅通过环境变化保持连续性。
- **对比方法**：评估三个智能体（通过OpenHands框架）：GPT-5-mini（闭源）、Claude Haiku 4.5（闭源）、Qwen3-Coder（开源）。额外用一个ablated Llama3.1-8B-Instruct模型（弱无对齐）作为非分解基线，验证执行难度准则。
- **评估指标**：攻击成功率（ASR，所有检查点通过的任务比例）、拒绝率（RR，明确拒绝的任务比例）、检查点通过率（CPR，通过检查点占所有检查点的比例）。失败分为安全拒绝和能力失败。
- **额外对比**：将GPT-5-mini在DeCompBench整体任务上的表现与OpenAgentSafety（仅恶意意图子集99个任务）对比，显示DeCompBench任务更难以拒绝率更高。

## 4. 资源与算力
- 所有实验在**AWS t3.2xlarge实例**（8 vCPU，32 GB RAM）上运行，每个任务需启动完整Docker服务栈（GitLab、PostgreSQL、OwnCloud等），且必须顺序执行（避免端口冲突和状态污染）。
- 评估一个模型：整体设置约**15小时**，分解设置约**36小时**。
- 闭源模型通过外部API调用，开源模型Qwen3-Coder和ablated Llama在**NVIDIA A6000 GPU**上运行。
- 论文未报告具体GPU数量或训练步数，因为未进行模型训练，仅作评估。

## 5. 实验数量与充分性
- **实验数量**：核心实验涉及3个智能体×2种设置（整体/分解）=6组主要结果，每组包含250个任务的评估。另加一个ablated模型整体设置结果，以及与OpenAgentSafety的对比（表3）。实验覆盖所有8个攻击类别，每个任务有多个可验证检查点（平均每个任务约5.98个子任务）。
- **充分性与客观性**：
  - **优点**：评估维度全面（ASR、RR、CPR、失败原因分类），重复了多次独立任务，结果清晰显示分解攻击效应。使用程序化检查点和LLM评判检查点结合，避免主观偏差。基准任务经过手工验证，确保质量。
  - **不足**：未做消融实验（如不同分解策略、不同温度的影响），也未对分解器做敏感性分析。仅对比三个智能体，未包含更多模型（如其他开源或闭源模型）。未评估分解后子任务顺序或聚合方式的影响。整体实验设计合理但不够完整，未涉及防御方法的对比。

## 6. 论文的主要结论与发现
- **分解显著降低拒绝率**：所有智能体对分解任务的拒绝率大幅下降。例如，Claude Haiku 4.5从90.7%降至2.5%，GPT-5-mini从89.8%降至6.0%，Qwen3-Coder从20.9%降至0%。
- **分解显著提高攻击成功率**：Claude Haiku 4.5和GPT-5-mini从0%提升至约70%，Qwen3-Coder从16.6%提升至36.1%。检查点通过率也大幅提升（Claude哈库：8.9%→89.5%；GPT-5-mini：11.4%→85.4%）。
- **失败主因从安全拒绝变为能力限制**：分解设置下，未成功任务中仅少数因安全拒绝（GPT-5-mini 19%、Claude 8.3%、Qwen 0%），大部分是能力失败（智能体尝试执行但未能正确操作工具）。
- **仅移除安全对齐不足以保证成功**：ablated Llama模型整体任务拒绝率为0%，但攻击成功率为0%，检查点通过率仅约6%，表明任务本身具有非平凡难度（执行难度准则成立）。
- **现有安全机制失效**：安全对齐仅对整体有害提示有效，但当有害意图分布于多个单独无害子任务时无法检测。DeCompBench任务比OpenAgentSafety恶意任务更严苛（GPT-5-mini在OAS上58.6%成功，在DeCompBench上0%成功）。

## 7. 优点：方法或实验设计上的亮点
- **方法创新**：首次提出“分解即设计”构建可分解基准，确保任务存在真实有效分解，而非事后拼凑。图形框架（DAG）保证子任务依赖性和良性隔离。四阶段流水线系统化生产高质量任务。
- **现实相关性**：任务基于真实世界工具（GitLab、PostgreSQL等），包含NPC交互，分解器模拟真实攻击者策略（文件间接、逐步包裹），评估设置清空对话历史符合实际攻击场景。
- **评估全面**：同时报告攻击成功率、拒绝率、检查点通过率和失败原因分类，从多个维度揭示安全漏洞。
- **可复现性**：数据集公开于Hugging Face，并提供完整生成提示和评估流程，便于后续研究。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制
- **覆盖范围有限**：仅8个攻击类别，且分布不均（审计篡改和金融犯罪合计近50%）。仅英文任务，仅针对工具使用智能体，未覆盖其他类型智能体（如纯文本、多模态）。
- **环境依赖**：任务基于特定服务栈（如GitLab、PostgreSQL、OwnCloud等），其他部署可能面临不同风险，泛化性需验证。
- **攻击分类不完整**：可能未覆盖所有现实攻击模式或操作约束。分解器仅使用GPT-4o产生一种分解，不同分解策略的影响未研究。
- **实验公平性**：仅测试三个模型，样本量较小；未进行统计显著性检验。闭源模型API版本可能随时间变化，复现困难。未测试相同模型的不同温度或系统提示影响。
- **防御缺失**：论文仅聚焦评估，未提出或对比防御方法（如状态缓存、跨对话意图检测），实用性受限。
- **潜在滥用风险**：基准虽用于防御研究，但也可能被攻击者利用学习如何分解有害任务。作者承诺门控访问和附带防御指南，但风险仍存在。
- **计算资源消耗高**：顺序执行导致评估耗时，可能限制大规模基准更新。

（完）
